# LabBot 디자인 원칙

> 2026-08-26 개정: Voltagent(AI 에이전트 개발 플랫폼) 디자인 언어를 참고해서 톤을 다시 잡았다.
> 이전 개정(미니멀 라이트)은 이 문서로 완전히 대체한다. 근거 파일: `docs/design-references/DESIGN-voltagent.md`(참고용, 사용자 제공).

## Design Read

Reading this as: 대학 연구실 재고·안전관리 실무 도구(대시보드/데이터 테이블 중심) for 연구실 학생·관리자,
with a "개발자 도구 다큐먼트" 언어 — 짙은 캔버스 배경 + 헤어라인(가는 실선) 보더 + 단일 그린 액센트로
정보를 정확하게 전달하는 톤. 장식(그림자, 글로우, 그라데이션)은 없고, 모든 경계는 1px 헤어라인으로만
표현한다.

## 색상

**베이스는 다크 캔버스.** 근-검정 배경 위에 헤어라인 보더로 카드/표/입력창 경계를 긋는다. 강조색은
그린 하나만 쓴다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#101010` | 페이지 배경(캔버스) — 유일한 표면 톤, 라이트모드 없음 |
| `--surface` | `#101010` | 카드/표/입력창 배경 — 페이지와 같은 캔버스, 헤어라인만으로 경계를 만든다 |
| `--surface-2` | `#1a1a1a` | 코드/입력창처럼 캔버스와 살짝 구분돼야 하는 곳, 헤더 행, 호버 배경 |
| `--border` | `#3d3a39` | 헤어라인 보더 — 카드·버튼·표·구분선 전부 이 색 1px |
| `--border-soft` | `#2a2826` | 표 안 셀 구분선처럼 더 옅어야 하는 보더 |
| `--text` | `#f2f2f2` | 기본 텍스트 |
| `--text-strong` | `#ffffff` | 헤드라인, 고강조 텍스트 |
| `--text-muted` | `#bdbdbd` | 보조 텍스트, 본문 설명 |
| `--text-faint` | `#8b949e` | 캡션, 자리표시자, 가장 낮은 우선순위 텍스트 |
| `--accent` | `#00d992` | 유일한 브랜드 컬러 — CTA, 라이브 상태, 포커스링 전용 |
| `--accent-soft` | `#2fd6a1` | 고스트 버튼 텍스트, 링크 |
| `--accent-deep` | `#10b981` | 본문 안 링크 색 |
| `--accent-ink` | `#101010` | 그린 배경 버튼 위 텍스트(근검정) |

**색상 일관성 잠금**: `--accent`(그린) 하나만 강조색으로 쓴다. 본문에는 절대 그린을 채우지 않는다 —
CTA·라이브 표시 전용. 상태 배지(위험/경고/성공)만 예외로 red/amber/green을 쓰되, 항상 같은 의미로만
쓴다(빨강=위험/연체, 항상 동일).

**그림자를 쓰지 않는다.** 카드의 "떠 있는 느낌"은 그림자가 아니라 **헤어라인 보더**로만 만든다. 모달처럼
정말 다른 레이어 위에 뜨는 요소만 예외로 진한 그림자를 쓴다(아래 "고도(elevation)" 참고).

## 타이포그래피

- 본문/UI/제목 전부 **Inter** 하나로 통일한다 — 별도 디스플레이 폰트(Space Grotesk)는 더 이상 쓰지 않는다.
  Voltagent의 60px 히어로도 굵기 400(레귤러)로 차분하게 가는 것처럼, 큰 글자라고 굵게 만들지 않는다.
- 모노스페이스(코드/QR코드/수치/eyebrow 라벨): **JetBrains Mono** 유지(SF Mono 대체 폰트로 명시된 조합).
- 대문자 eyebrow 라벨(작은 대문자 카테고리 표시)은 Inter 600 굵기 + 넓은 자간(`letter-spacing: 0.15em`
  전후)으로 쓴다 — LabBot에서는 `.hero-eyebrow`, `.guide-eyebrow`, 표 헤더(`thead th`) 등이 해당.
- 세리프 폰트는 쓰지 않는다.

## 모양 (Shapes)

경계 반경을 역할별로 두 단계만 쓴다 — 하나로 뭉치지 않는다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--radius-sm` | `6px` | 버튼, 입력창 |
| `--radius` | `8px` | 카드, 표, 모달, 토스트 |
| pill | `9999px` | 상태 배지, 카테고리 필터 칩처럼 완전히 둥근 것만 |

## 고도 (Elevation) — 그림자 대신 헤어라인

| 단계 | 처리 | 쓰는 곳 |
|---|---|---|
| 기본 | 그림자 없음, 헤어라인 1px만 | 카드·표·입력창·버튼 전부의 기본값 |
| 강조 | 헤어라인을 `--accent`로 교체(2px) | 활성 탭, 선택된 카테고리, "지금 이 카드가 중요함" 표시 |
| 모달/오버레이 | 진한 그림자(`0 20px 60px rgba(0,0,0,0.6)`) | `.modal-card`, 토스트처럼 다른 레이어 위에 뜨는 요소만 |

## 컴포넌트 규칙

- **버튼**: `--radius-sm`(6px). Primary는 그린 배경 + 근검정 텍스트, Secondary는 캔버스 배경 + 헤어라인
  보더 + 기본 텍스트, Ghost는 배경 없이 그린 텍스트만(`--accent-soft`).
- **카드**(재고행, 대여카드, 요약카드 등): 배경은 캔버스와 같은 `--surface`, 구분은 1px `--border` 헤어라인
  으로만. 그림자 금지.
- **표**: 헤더 행은 `--surface-2`, 셀 구분선은 `--border-soft`. 헤더 라벨은 eyebrow 스타일(모노, 대문자,
  자간 넓게) 그대로 유지.
- **배지/상태 태그**: pill 모양, 헤어라인 보더 + 옅은 배경 틴트 + 진한 텍스트. red/amber/green 의미는
  기존과 동일하게 유지.
- **입력창**: `--surface-2` 배경(캔버스보다 살짝 밝게), `--radius-sm`, 포커스 시 `--accent` 보더 + 옅은
  그린 링(`box-shadow: 0 0 0 3px rgba(0,217,146,0.15)` — 이건 글로우가 아니라 포커스 접근성 링).
- **토스트/모달**: 유일하게 그림자를 쓰는 곳. 모달은 진한 드롭섀도, 토스트는 중간 정도.

## 모션

- 기본은 정적. 페이지 전환 페이드(200ms), 토스트 등장/사라짐, 탭 전환 정도만 유지.
- 무한 반복되는 장식용 애니메이션(파티클, 펄스 글로우, 스캔라인)은 쓰지 않는다.
- 상태를 전달하는 최소한의 펄스(예: "검토 필요" 배지 점 깜빡임)만 예외로 허용 — 장식이 아니라 정보이기
  때문.

## 예외

- 관리자 페이지도 이 원칙을 그대로 따른다.
- Safety 이벤트 심각도, 재고상태 배지 등 **의미를 전달하는 색**(red/amber/green)은 계속 쓴다 — 장식이
  아니라 정보다.
- QR 스캔 프레임처럼 기능적으로 필요한 어두운 오버레이(`box-shadow: 0 0 0 999px rgba(0,0,0,.35)`,
  카메라 프레임 밖을 어둡게 하는 용도)는 "장식용 글로우"가 아니므로 유지한다.
