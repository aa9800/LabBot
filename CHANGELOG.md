# CHANGELOG

이 파일은 지우지 않고 계속 누적합니다. **커밋/푸시할 때마다 맨 위에 새 항목을 추가**하세요
(날짜 + 무엇을 왜 바꿨는지 한두 줄). 오래된 항목도 지우지 않습니다 — 전체 이력이 남아있어야
나중에 "언제부터 이게 이랬지?"를 추적할 수 있습니다.

---

## 2026-08-28 (59) — 발표 전 마감 다듬기 (전체 점검에서 나온 항목들을 하나씩)

전체 점검(디자인/기능 감사)에서 나온 "발표 전 꼭" 목록을 사용자와 하나씩 확인하며
진행 중. Robot Console 버튼 스타일(1번)은 사용자 요청으로 제외.

- **홈 히어로의 빈 공간 제거** — `index.html`에 `<p class="hero-desc"></p>`가 내용 없이
  남아 있고 CSS가 `min-height: 2.7em`으로 그 자리를 지켜서, 제목과 버튼 사이가 **84px**
  비어 있었다(원래 문구를 지웠을 때 간격이 갑자기 좁아지지 않게 하려고 일부러 남긴
  것이었는데, 결과적으로 "만들다 만 화면"처럼 보였다). 빈 `<p>`와 `.hero-desc` 규칙을
  통째로 걷어내고 `.hero-title`의 아래 여백만 28px로 잡았다.
  검증: 브라우저 실측 — 제목↔버튼 간격 **84px → 31px**, 빈 요소 제거 확인.

- **비활성 버튼 스타일 추가** — `.btn`에 `:disabled` 규칙이 아예 없어서, 브라우저 기본
  회색이 `.btn-secondary`의 `color: var(--text)`에 밀렸다. 그 결과 "재고없음"/"대여불가"
  버튼이 바로 옆의 살아있는 "예약하기"와 **똑같아 보였다**(`items.js:57`,
  `chat-widget.js:290`, `chatbot.js:103`). `.btn:disabled { opacity:.45; cursor:default }`를
  추가(cursor는 기존 `.link-btn:disabled` 관례에 맞춤). 더불어 `.btn-primary:hover` /
  `.btn-secondary:hover` / `.btn-danger:hover` / `.btn:active`에 `:not(:disabled)`를 붙여서,
  비활성 버튼에 마우스를 올렸을 때 색이 변해 "아직 누를 수 있는 것처럼" 보이던 것도 막았다.
  요청 처리 중 잠깐 비활성이 되는 버튼(예약/전송/페이지네이션 이전·다음)에도 그대로 적용된다.
  검증: `items.js:57`이 만드는 마크업을 그대로 주입해 computed style 비교 — 비활성
  opacity **0.45**/cursor default, 활성 opacity **1**/cursor pointer. 로드된 스타일시트를
  CSSOM으로 훑어 버튼 hover/active 규칙이 전부 `:not(:disabled)`로 스코프된 것도 확인.

- **가상 실험실 페이지 폭 제한 해제** — `.lab-twin-container`는 `max-width: 1400px`인데
  부모인 `.page`가 1080px이라 실제로는 1080px에 갇혀 있었다. 2.5D 뷰포트와 360px 고정
  인스펙터가 나란히 놓이는 레이아웃이라, 페이지가 좁으면 그 차이를 캔버스가 전부 떠안는다.
  이미 있던 `body[data-page="admin"] .page { max-width: 1440px }`와 같은 패턴으로
  `body[data-page="lab-twin"] .page`를 추가했다(좌우 패딩 20px씩이라 1440px면 내용 폭이
  정확히 1400px가 되어 컨테이너 값과 맞아떨어진다).
  검증: 1600px 뷰포트에서 수정 전/후를 같은 페이지에서 직접 비교 — 캔버스 폭
  **691px → 1087px (+57%)**, 인스펙터는 396px 그대로(고정 트랙이라 안 늘어나는 게 정상),
  가로 스크롤 없음. 1280px(노트북)에서도 2단 유지·가로 스크롤 없음을 확인했고,
  1024px 이하는 기존 `virtual-lab.css` 미디어쿼리가 1단으로 접으므로 영향 없음.

- **모달이 화면 위아래에 딱 붙던 것 수정** — `.modal-card`의 `max-height: 90vh`가 이 파일에서
  **유일하게 `--zoom` 보정이 빠진 vh**였다. 관리자 페이지를 뺀 전체가 `zoom: 1.1`인데 vh는
  확대 전 기준이라, 90vh가 실질 99vh로 그려져 오버레이가 주려던 20px 여백이 사라졌다.
  `calc(90vh / var(--zoom, 1))`로 바꿔 파일 내 다른 vh들(`:1442` 등)과 방식을 통일.
  적용 대상: 파손 신고(`mypage.js:387`), 문의하기(`inquiry-widget.js:38`), 관리자 팝업 4개
  (`admin.js:288/352/1931/2000`).
  *처음 보고할 때 "내용이 잘리고 스크롤로도 못 본다"고 했는데 실측해보니 틀렸다 — 항상
  `0.99 × 화면높이`라 잘리지는 않는다. 실제 증상은 여백이 사라져 가장자리에 붙는 것.*
  검증: 650px 높이 창 + 긴 모달 기준 위/아래 여백 **3px → 33px**, 카드 내부 스크롤로 모든
  내용 도달 가능(`cardScrolls: true`) 확인. 짧은 모달은 기존처럼 정중앙 배치 유지.

- **가상실험실 나비 라벨 띄어쓰기 통일** — `lab-twin.html`만 "가상 실험실"(공백 있음)이고
  나머지 8개 페이지는 "가상실험실"이라, 그 페이지에 들어가는 순간 메뉴 글자가 미묘하게
  바뀌었다. 붙여쓰기로 통일 — 9개 페이지 전부 일치 확인.

- **이모지 아이콘(✉️ 💬 🔍 🔎)을 SVG 아이콘 체계로 교체** — 사이트의 다른 아이콘(로고,
  관리자 자물쇠, 이용방법 3단계, 가상실험실 물품 노드)은 전부 `viewBox="0 0 24 24"` +
  `fill="none"` + `stroke="currentColor"` 규격의 단색 SVG인데, FAB 두 개와 검색창 돋보기만
  풀컬러 이모지라 혼자 튀었다(OS/브라우저마다 모양도 제각각이라 발표 PC에서 어떻게 보일지
  보장이 안 됐다). 같은 규격의 SVG로 교체:
  - **FAB 2개**(문의하기 ✉️ → 봉투, 챗봇 💬 → 말풍선+점3개) — 6개 페이지에 동일하게 들어
    있어서 스크립트로 일괄 교체(12곳 전부 확인). `currentColor`라 버튼 배경색에 맞춰
    자동으로 대비되는 색이 된다.
  - **검색창 돋보기 🔍** — `::before`의 `content`에는 SVG 마크업을 넣을 수 없어서 `mask`로
    처리했다. data URI에 색을 박아넣는 방식과 달리 `background-color: var(--text-faint)`를
    입히는 구조라 라이트/다크 테마를 그대로 따라간다.
  - **관리자 QR 인식 버튼 2곳**(`admin.html`, `admin.js`의 재렌더 포함)과 **챗봇 안내문 🔎**
    (독립 페이지 `chatbot.html` + 팝업 위젯 `chat-widget.js` 양쪽).
  - 아이콘+글자 버튼을 위해 `.btn`에 `gap: 6px` 추가(글자만 있는 버튼에는 영향 없음),
    `.chat-guide`를 flex로 바꾸고 아이콘에 `flex-shrink: 0`(문구가 줄바꿈될 때 찌그러짐 방지).
  검증: FAB 2개에 이모지 잔여 텍스트 0, SVG가 버튼 색(`currentColor`)을 상속하는 것 확인.
  돋보기는 dark/light 전환하며 아이콘 색이 `--text-faint`를 따라 바뀌는 것 확인
  (`#8b949e` ↔ `#82857b`). `.btn` gap 6px 적용 및 글자 전용 버튼 레이아웃 무영향 확인.
  `node --check`로 `admin.js`/`chat-widget.js` 문법 검증.

  **남겨둔 것**: 관리자 화면의 다른 이모지(🔄 자동 순찰 복귀, ✅ 재고 실사 확정, ■ 강제 정지)는
  이번 요청 범위(✉️💬🔍) 밖이라 그대로 뒀다.

## 2026-08-28 (58) — 🔴 가상 실험실 페이지가 통째로 렌더링되지 않던 치명적 버그 수정

전체 점검 중 발견. `virtual-lab.js`가 `LabBotAuth.currentSession()`을 호출하는데,
`auth.js`가 실제로 내보내는 이름은 **`getSession`**이다(`currentSession`은 존재하지
않는다). 게다가 이 호출이 `loadUserLoans()`의 try 블록 **바깥**에 있어서, TypeError가
`initVirtualLab()`까지 그대로 올라가 그 뒤의 `renderLabCanvas()` /
`setupEventListeners()` / `subscribeRealtime()`가 **한 줄도 실행되지 않았다.**

**증상**: 가상 실험실 페이지에 들어가면 2.5D 캔버스가 완전히 빈 화면. 물품 노드,
실험대, 유리벽, 로봇 마커가 전부 안 보이고, 노드 클릭·예약·경로안내도 전부 무반응.
에러 메시지도 안 뜨고 조용히 죽어 있었다.

**왜 여태 못 잡았나**: (49)~(53)번에서 이 페이지 디자인을 계속 고쳤는데, 그때 브라우저
콘솔에 `LabBotAuth.currentSession is not a function` 에러가 매번 떠 있었지만 로그인이
안 된 탓이라고 넘겨짚고 지나쳤다. 유리벽 색을 고칠 때도 `.env-glass-wall` 개수가 0이라
테스트용 엘리먼트를 직접 주입해서 확인했는데, 그게 바로 이 버그의 증상이었다 —
**즉 (51)~(53)번의 디자인 수정은 지금까지 화면에 반영된 적이 없었다.**

수정: 두 호출 지점(`loadUserLoans`, `handleReserveClick`)을 `getSession()`으로 바꾸고,
세션 조회를 try 안으로 옮겨서 앞으로 세션 조회가 실패해도 캔버스 렌더까지 막히지 않게 했다.

검증: 브라우저에서 실제로 확인 — 캔버스 innerHTML이 **0자 → 8,306자**, 물품 노드 11개,
유리벽 2개, 실험대 3개, Raspbot 마커 1개, 구역 탭 10개가 정상 렌더링됨.

## 2026-08-28 (57) — 관리자 "통계" 탭 추가 (장비 대여 사용량 / 카테고리별 대여 / 연체율 / 실사 정확도 추이) + 시연용 샘플 데이터

설계문서 §04에 있던 "통계 대시보드"가 실제로 구현이 안 돼 있던 걸(팀 진행상황 점검 중
발견) 관리자 일상 운영용 범위로 우선 추가했다. 새 테이블/RPC 없이 이미 있는
`fetchAllLoans()`(items 조인 포함)와 `fetchAuditSessions()`만 다시 집계해서 만들었다 —
차트 라이브러리 없이 숫자 카드 + 표 위주로(사용자가 직접 선택한 방향).

- `web/admin.html` — 탭 목록에 "통계" 추가, `#tab-stats` 패널 신설. 요약 카드 3개(누적
  대여 건수/현재 대여중 연체율/최근 실사 정확도) + 표 3개(장비 대여 사용량, 카테고리별
  대여 현황, 최근 실사 5건 정확도 추이 — 전체 이력은 기존 "재고 실사" 탭으로 안내).
- `web/js/admin.js`의 `renderStatsPanel()` — `loans.status !== "취소됨"`으로 취소 예약
  제외(마이페이지 총 대여횟수 집계와 같은 기준), 연체율은 반납완료건을 섞지 않고
  "현재 대여중" 건만 놓고 계산(요약 카드의 `summaryOverdueUsers`와 같은 관점). `TAB_RENDERERS`에
  등록해 탭 전환마다 새로고침되게 하고, `showPanel()` 초기 로드에도 포함시켰다.
- `web/css/style.css` — `.stats-grid`/`.stats-section-title`/`.stats-scroll-table` 추가
  (기존 `.summary-card`/`.table-wrap`는 그대로 재사용).

**사용자 피드백 반영**: 제목을 "인기 장비 Top 5" → **"장비 대여 사용량"**으로 바꾸고,
Top 5로 자르던 것도 없앴다(적게 쓰이는 장비도 추가구매/처분 판단에 필요한 정보라서).
목록이 길어질 수 있어 그 표에만 세로 스크롤(`.stats-scroll-table`, max-height 320px)을 줬다.

**검증 중 발견해서 고친 버그**: 카테고리별 비율의 분모를 `validLoans.length`로 쓰고
있었는데, 물품이 삭제된 대여 건(`items`가 null)은 어느 카테고리에도 안 들어가면서
분모에만 남아서 비율 합이 100%가 안 됐다(테스트에서 75%+13%=88%). 분모를 "카테고리를
알 수 있는 건수의 합"으로 바꿔서 86%+14%=100%가 되는 것을 확인.

**검증 방법**: 관리자 로그인 계정이 없어 화면으로는 못 봤지만, `admin.js`에서
`renderStatsPanel()` 본문을 그대로 추출해 Node에서 실제로 실행했다(로직을 베껴 쓴 게
아니라 실제 소스를 eval). 픽스처로 취소된 예약 1건, 물품이 삭제된 대여 1건, 연체
2건을 섞어 넣고 집계 결과를 전부 대조 — 누적 8건(취소 제외 정상), 연체율 67%(대여중
3건 중 2건, 정상), 사용량 정렬·카테고리 비율·실사 정확도 추이 모두 기대값과 일치.
HTML↔JS DOM id 6개도 전수 대조 완료.

### 함께 추가: `docs/labbot_schema.sql` 35번 섹션 — 통계 시연용 샘플 데이터

통계 탭이 빈 표로 보이지 않게, 대여 이력 90건 + 재고 실사 5회를 넣는 스크립트를 추가했다
(스키마 변경이 아니라 데이터 삽입이라 **선택 실행**, 되돌리기 쿼리도 같이 적어둠).
기존 profiles/items를 그대로 참조하고, 소수 장비에 대여가 쏠리도록 분포를 기울여서
"장비 대여 사용량" 표가 의미 있는 곡선을 그리게 했다. 실사 정확도는 78→96%로 올라가는
추이로 넣었다.

주의해서 처리한 부분 — loans에는 INSERT 트리거가 **세 개** 걸려 있어서 그냥 INSERT하면
안 된다: `trg_guard_loan_insert`(재고 0/점검중/만료 거부), `trg_guard_loan_self_insert`
(25번 섹션 — SQL Editor에서는 `auth.uid()`가 NULL이라 `is_admin()`이 false가 되면서
과거 이력 삽입이 **전부 막힌다**), `trg_loans_decrement_stock`(이미 반납된 과거 이력까지
재고를 깎아 재고를 영구히 어긋나게 만든다). 삽입 동안만 셋 다 끄고, '대여중'/'예약중'
처럼 지금도 물건을 쥐고 있는 건에 대해서만 `available_qty`를 직접 -1 해서 재고 정합성을
맞췄다.

**확인하지 못한 것**: 이 환경에는 Postgres가 없어서 이 SQL을 **실제로 실행해보지는
못했다** — 트리거/제약조건/컬럼 정의를 스키마에서 일일이 대조해 작성했지만, Supabase
SQL Editor에서 처음 돌릴 때는 통째로 한 번에(트랜잭션으로) 실행하는 걸 권장한다.

## 2026-08-28 (56) — 문의하기 버튼 다시 누르면 팝업이 토글로 닫히게 수정 (+ 실제로 안 눌리던 z-index 버그까지 수정)

(55)번에서 배경 클릭 닫기를 빼고 나니, 문의하기 FAB 버튼을 두 번 누르면 오버레이가
겹쳐 쌓이는 문제가 생겼다(닫는 경로가 취소 버튼과 제출 성공뿐이라 버튼을 또 눌러도
그냥 새 모달을 하나 더 열었음). 사용자 요청대로 FAB 버튼을 다시 누르면 열려있는
모달을 닫도록 토글 처리 — 모달 오버레이에 `data-modal="inquiry"`를 표시해두고,
버튼 클릭 시 이 오버레이가 이미 있으면 세션 확인 없이 바로 제거하고 리턴한다.

**1차 수정으로는 실제로 안 고쳐졌었다** — 사용자가 "수정이 안 된 것 같다"고 재확인해줘서
다시 봤더니, 토글 로직 자체는 맞았지만 `.modal-overlay`(z-index:300)가 화면 전체를
덮고 있고 `.fab`(z-index:200)는 그보다 낮아서, 모달이 열려있는 동안 버튼이 오버레이
밑에 깔려 클릭이 버튼까지 닿지 않았다(`.click()`으로 직접 호출하는 테스트는 실제
브라우저 히트테스트를 건너뛰어서 이 문제를 못 잡아냈다). 모달이 열려있는 동안만
버튼의 z-index를 301로 올렸다가, 닫힐 때(취소/제출성공/토글) 원복하도록 수정.

검증: `document.elementFromPoint()`로 버튼 좌표의 실제 히트테스트 결과를 확인 —
모달이 열려있는 동안에도 그 좌표에서 실제로 버튼 엘리먼트가 잡히는 것(오버레이가
아니라)을 확인했고, 닫힌 뒤 버튼 z-index가 원래 스타일시트 값(200)으로 정상
복귀하는 것도 확인.

## 2026-08-28 (55) — 문의하기 팝업, 배경 클릭으로 안 닫히게 수정

사용자 요청("문의하기 팝업에서 밖 웹페이지를 클릭하면 사라지는 현상을 빼주고 취소/보내기
버튼을 눌러야 닫히게"). `inquiry-widget.js`의 `openInquiryModal()`에 있던
`overlay.addEventListener("click", ...)`(배경 클릭 시 닫힘)와 Escape 키 리스너를 제거 —
이제 `[data-action="close"]`(취소) 버튼과 제출 성공 시 호출되는 `close()`로만 닫힌다.
입력하던 중 실수로 바깥을 눌러 작성 내용이 날아가는 걸 막기 위함.

검증: 브라우저에서 `openInquiryModal()`을 직접 호출해 오버레이 배경 클릭 시 안 닫히고,
취소 버튼 클릭 시에는 정상적으로 닫히는 것을 확인.

## 2026-08-28 (54) — 마이페이지에서 내 문의 삭제(=본인 목록에서 숨기기) 기능 추가

사용자 요청("마이페이지에서 내 문의 내역을 삭제 가능하게 수정해줘"). 처음엔 DB
row를 실제로 지우는 delete 정책으로 만들었는데, 사용자가 "관리자는 계속 전체 이력을
봐야 하니 실제 DB 삭제가 아니라 본인 마이페이지 화면에서만 안 보이게 해달라"고
방향을 바꿔서 soft-hide 방식으로 다시 설계했다.

- `docs/labbot_schema.sql` 34번 섹션 — `inquiries`에 `hidden_by_user boolean default
  false` 컬럼 추가 + `hide_my_inquiry(p_inquiry_id)` RPC(security definer, 본인 소유
  확인 후 hidden_by_user만 true로 세움). 별도 update RLS 정책을 열지 않고 이 RPC
  하나로만 길을 좁힌 이유: subject/message/status 등 다른 컬럼까지 사용자가 직접
  고칠 수 있는 문을 열면 27번 섹션의 "본인 글이라도 직접 못 고치게(위변조 방지)"
  원칙이 깨지기 때문 — **Supabase SQL Editor에서 이 섹션을 직접 실행해야 적용된다.**
- `web/js/inquiry-data.js` — `fetchMyInquiries()`에 `hidden_by_user = false` 조건 추가
  (숨긴 문의는 본인 목록에서 제외), `deleteInquiry` 대신 `hideMyInquiry(inquiryId)`
  추가(RPC 호출) — `fetchAllInquiries()`(관리자용)는 그대로 둬서 관리자는 숨긴 문의도
  포함한 전체 이력을 계속 본다.
- `web/js/mypage.js`의 `renderInquiries()` — 문의 카드에 "삭제" 버튼 추가
  (`.safety-actions`/`btn-secondary btn-sm`, 예약취소 등과 같은 스타일), 클릭 시
  `confirm()` → `hideMyInquiry()` 호출 → 토스트 → 목록 재조회.

**확인하지 못한 것**: 이 환경에 로그인 가능한 테스트 계정이 없어서, RPC가 실제 DB에서
정상 동작하고 목록이 갱신되는 것까지는 확인하지 못했다 — `node --check`로 문법만
검증. 위 스키마 섹션을 Supabase에 직접 실행한 뒤 실제 동작을 한 번 확인해보는 걸 권장.

## 2026-08-28 (53) — 가상 실험실 유리 파티션/유리벽 라벨 텍스트도 다크 모드 고정

사용자 요청("캔버스에 보여지는 '기기실 유리파티션', '세포배양실 클린 유리벽'
부분도 다크모드와 동일하게"). `virtual-lab.css`의 `.env-glass-wall`이 배경/테두리는
고정 rgba값을 쓰면서도 글자색(`color`)은 따로 지정하지 않아서, 상위 페이지의
`var(--text)`를 그대로 물려받고 있었다 — 다크 모드에서는 `--text`가 밝은 회색
(#f2f2f2)이라 괜찮아 보였지만, 라이트 모드에서는 `--text`가 어두운 색(#24261f)으로
바뀌면서 어두운 뷰포트 배경 위에 어두운 글씨가 겹쳐 거의 안 보이는 상태가 됐다.
`.env-workbench` 라벨과 같은 고정 색(`#94a3b8`)을 명시적으로 지정해서 (52)번과
같은 원칙(뷰포트 안쪽은 테마 무관하게 항상 다크 모드 배색 유지)으로 통일.

검증: 테스트 엘리먼트를 뷰포트 안에 주입해 `data-theme`을 dark/light로 전환하며
computed color 비교 — 양쪽 모두 `rgb(148, 163, 184)`로 동일함을 확인.

## 2026-08-28 (52) — 가상 실험실 2.5D 뷰포트 색상을 다크 모드로 완전 고정

(51)번 작업에서 뷰포트 배경/테두리(#1e293b/#334155)는 이미 고정값으로 남겨뒀지만,
그 안의 강조색(OSD 뱃지, 로봇 유도선, 안전 캐비닛, 선택된 물품 노드 테두리/상태
점, Raspbot 로봇 마커/라벨)은 `var(--accent)`/`var(--warning)`/`var(--danger)`
같은 테마 변수를 쓰고 있어서, 라이트 모드로 전환하면 뷰포트 "화면 속 화면"만
색이 미묘하게 바뀌는 문제가 있었다. 사용자 요청("캔버스 자체는 라이트 모드,
다크모드 동일하게 다크모드로 적용해줘")에 따라 뷰포트 내부에서 쓰이는 색상을
전부 다크 모드 리터럴 값(#00d992/#2fd6a1/#fbbf24/#f87171/#8b949e 등)으로 교체 —
`virtual-lab.css`의 `.osd-badge`/`.robot-track-line`/`.robot-guide-active`/
`.env-safety-cabinet`/`.lab-item-node.highlighted .node-box`/`.raspbot-body`/
`.raspbot-label`, `virtual-lab.js`의 노드 상태색(`statusColor`)과 선택 테두리색,
`lab-twin.html`의 OSD 뱃지 인라인 스타일까지 수정. 헤더 카드/인스펙터 패널/버튼
등 뷰포트 바깥 요소들은 (51)번 그대로 테마에 반응하도록 유지 — 뷰포트 안쪽만
의도적으로 다크 모드 고정.

검증: 브라우저에서 `data-theme`를 light로 강제 전환한 뒤 뷰포트 배경/테두리/
OSD 텍스트 색의 computed style을 다크 모드일 때와 비교 — 완전히 동일
(`rgb(30,41,59)`/`rgb(51,65,85)`/`rgb(0,217,146)`). 동시에 헤더 카드/인스펙터
패널 배경은 라이트 모드 값(`rgb(242,242,240)`)으로 정상 전환되는 것도 확인.

## 2026-08-28 (51) — 가상 실험실 페이지 전체 디자인 통일 (사이트 공통 디자인 토큰 적용)

사용자 요청("가상 실험실 페이지도 나머지 페이지와 동일한 디자인으로 적용해줘").
`lab-twin.html`/`virtual-lab.css`/`virtual-lab.js` 전체를 훑어서, 원래 이 페이지가
독자적으로 쓰던 하드코딩된 파란/남색 계열 hex 색상과 box-shadow를 전부 사이트의
실제 디자인 토큰(`var(--surface)`, `var(--border)`, `var(--radius)`, `var(--text)`,
`var(--accent)` 등)으로 교체했다.

- **카드/레이아웃**: `.lab-header-card`/`.lab-inspector-card`를 `var(--surface)` +
  1px `var(--border)` + `var(--radius)`로 통일하고 box-shadow 제거 (사이트 규칙:
  모달 외에는 그림자 없이 헤어라인 테두리만 사용).
- **구역 필터 탭**(`.room-tab-btn`)과 **검색창**(`.lab-search-input`)을 물품목록
  페이지의 `.category-filter-btn`/`.location-filter-select`와 픽셀 단위로 동일하게
  재작성 (mono 폰트, pill 반경, `--accent-soft`/`--accent-text`/`--accent-border`
  active 상태, focus ring 포함).
  - 실측 확인: 브라우저에서 `.room-tab-btn.active`와 `.category-filter-btn.active`의
    computed style을 비교 — 완전히 일치.
- **상태 표시 dot/뱃지**(재고 가능/부족/품절/점검, OSD 뱃지, 인스펙터 룸 뱃지)를
  전부 파란/초록 하드코딩 색상에서 `var(--accent)`/`var(--warning)`/`var(--danger)`/
  `var(--text-faint)` 체계로 변경.
- **버튼**: `.btn-virtual-reserve`/`.btn-virtual-scan`은 `.btn-primary`와,
  `.btn-virtual-route`는 `.btn-secondary`와 동일한 색·hover 규칙으로 재작성.
- **가상 스캔 모달**(`.virtual-scanner-overlay`/`.virtual-scanner-modal`)을 실제
  `.modal-overlay`/`.modal-card`와 동일하게(오버레이 `rgba(0,0,0,0.6)`+z-index 300,
  카드에 `var(--shadow)` 유지 — 모달은 사이트 규칙상 그림자 예외) 재작성.
- **`virtual-lab.js`의 인라인 스타일**(상태별 색상 변수, 선택 노드 테두리, 비활성
  버튼, 스캐너 모달 텍스트, 수량 입력창)도 전부 하드코딩 hex → `var(...)`로 교체.
  `var()`가 동적으로 생성되는 인라인 `style` 속성 안에서도 정상적으로 테마에
  반응하는 것을 브라우저에서 실측 확인.
- **의도적으로 그대로 둔 것**: 2.5D 캔버스 뷰포트(`.lab-viewport-card`)의 배경/
  테두리 hex(#1e293b/#334155)는 라이트/다크 테마와 무관하게 "항상 어두운 모니터
  화면" 느낌을 유지하려고 일부러 고정값으로 남겨둠 — 다만 그 안의 강조색(선택된
  노드 하이라이트, 로봇 마커, OSD 텍스트, 안전 캐비닛 표시)은 기존 파란/보라
  계열 대신 사이트의 초록 `--accent` 계열로 통일했다.
- **부수적으로 발견한 버그 수정**: `virtual-lab.js`에서 로그인 필요 안내 시
  `LabBotToast.show(msg, "warning")`를 호출하고 있었는데, 이 사이트 토스트에는
  `.toast-warning` CSS 클래스가 아예 없어서(존재하는 건 success/error/info뿐)
  스타일이 전혀 안 먹는 상태였다 — 의도한 노란색(`--warning`)과 매핑되는
  `"info"` 타입으로 교체.

검증: WCAG 대비율 스캐너를 `.lab-twin-container` 전체에 다시 돌려서 4.5:1 미만
요소 0개 확인(이전 (50)번 항목에서 남겨뒀던 3.5~4:1대 항목들도 이번 토큰 교체로
자동 해결됨). `node --check`로 `virtual-lab.js` 문법 확인.

## 2026-08-27 (50) — 가상 실험실 인스펙터 패널 텍스트-배경 대비 수정

사용자 요청("배경색 대비 안 보이는 글씨들을 수정해줘"). 실제로 WCAG 대비율을
계산하는 스크립트를 페이지에 직접 돌려서 확인 — 우측 "물품 상세정보" 패널의
제목(`#inspectTitle`)과 정보 값 4개(분류/재고상태/보관조건/유효기간)가 대비율
**1.14:1**(사실상 안 보이는 수준, WCAG 최소 기준 4.5:1)이었다. 원인:
`web/css/virtual-lab.css`의 `.inspector-header h3`/`.info-value`/`.info-label`이
`var(--surface, #ffffff)`처럼 변수를 쓰는 옆 규칙들과 달리 `color: #1e293b`
(짙은 남색)를 그대로 박아둬서, 실제로는 이 앱의 다크 테마 `--surface`(#1e1e22,
어두운 카드 배경) 위에 짙은 남색 글씨가 얹히는 조합이 됐다 — 아마 흰 배경을
가정하고 만든 스타일이 이 사이트의 다크 테마와 안 맞았던 것으로 보인다.
`var(--text, #1e293b)`/`var(--text-muted, #64748b)`로 바꿔서 다크/일반 모드
양쪽에서 자동으로 맞는 색을 쓰도록 수정 — 수정 후 재측정하니 두 항목 다 4:1대
이상으로 정상화됨.

**참고로 남겨둔 것**: `#inspectStock`(인라인 `style="color:#0284c7"`)과 인스펙터
빈 상태 안내문(`virtual-lab.js`의 인라인 `style="color:#64748b"`)은 대비율이
3.5~4:1 정도로 완전히 안 보이는 수준은 아니지만 WCAG 권장(4.5:1)에는 못 미친다 —
인라인 스타일이라 이번엔 손대지 않았고, 필요하면 추가로 다듬을 수 있음.

## 2026-08-27 (49) — 가상 실험실 구역 필터 탭 가로 스크롤 제거

사용자 요청("일반실험실/기기실.../세포배양실 부분 스크롤 없애줘. 2줄로 나와도
괜찮아"). `web/css/virtual-lab.css`의 `.room-filter-tabs`가 `overflow-x: auto`
+ 줄바꿈 없음이라 10개 구역 탭이 한 줄에 다 안 들어가면 가로 스크롤이 생기던
문제 — `flex-wrap: wrap`으로 바꿔서 넘치면 자연스럽게 다음 줄로 넘어가게 했다.
실제로 로드해서 가로 스크롤 없음(`scrollWidth === clientWidth`)과 10개 버튼이
2줄로 배치되는 것을 좌표로 확인.

## 2026-08-27 (48) — 대여 진행률 바 신규 추가 (화면 하단 중앙, 실제 대여 데이터 기반)

사용자 요청("대여 신청을 했을때 진행률에 따라 따로 표기, 위치는 챗봇버튼과 같은
라인의 위치하고 웹페이지 중간에"). `loans.status`엔 예약중/대여중/반납완료 세
상태만 있고 "로봇 안내 받는 중"은 따로 저장되지 않으므로(모달을 열어둔 순간의
화면 상태일 뿐), 실제 서버 데이터만으로 정직하게 3단계를 표현하도록 설계했다 —
예약 건이 있으면 1단계 완료, 예약중인 동안 계속 2단계(로봇안내·QR대기)로 표시,
대여중으로 바뀌는 즉시 3단계 완료로 보고 바 자체를 숨긴다.

새 `web/js/rental-progress-widget.js` — 로그인한 사용자의 예약중(수령대기) 건이
있을 때만 화면 하단 중앙에 뜨는 알약 모양 바를 만든다. 클릭하면 마이페이지로
이동. `web/css/style.css`에 `.rental-progress-bar` 등 스타일 추가, FAB 버튼과
같은 `bottom:20px`(같은 라인)에 `left:50%`로 중앙 배치. 로그인 화면 6개 페이지
(index/mypage/items/mobile-status/signup/login)에 스크립트 태그 추가.

**실측으로 잡은 버그**: 데스크톱에서는 문제 없었지만, 375px 모바일 폭에서 중앙
정렬한 바가 우측 FAB 두 개와 실제로 겹치는 것을 좌표로 확인 — 모바일에서만
`bottom:156px`(기존에 모바일 토스트도 같은 값으로 FAB를 피하던 것과 동일한
여백)로 올려서 겹침을 없앴다. 데스크톱은 요청하신 대로 "같은 라인" 그대로 유지.

로그인 세션이 없는 이 환경에서는 실제 예약 데이터로 바가 뜨는 것까지는 검증하지
못했고, 동일한 마크업을 직접 주입해 위치·반응형·FAB 비겹침만 좌표로 확인했다.

## 2026-08-27 (47) — 홈 화면 "이용 방법"을 세그먼트 탭 바로 교체(교수님 피드백)

사용자 요청("이용방법을 바형태로 수정") — 카드 3개를 나란히 두던 그리드 대신,
1·2·3 단계를 가로 막대 하나에 3등분한 탭 바(`.step-tabbar`)로 바꾸고, 클릭한
단계의 아이콘+설명만 그 아래(`.step-detail`)에 펼쳐 보여주도록 했다. 대여/반납
토글(기존 `.usage-toggle`)은 그대로 두고, 그 안쪽 3단계 표현 방식만 교체 —
두 흐름 각각 독립된 탭 바를 가지므로 서로 간섭하지 않는다.

이제 안 쓰는 카드 그리드 전용 CSS(`.step-card`, `.step-number`, `.step-title`,
`.usage-steps.active`의 그리드 규칙, 그리고 이미 마크업에서 안 쓰인 지 오래된
`.steps-row`/`.step-arrow`까지)를 함께 정리해서 지웠다. `web/index.html`,
`web/css/style.css`, `web/js/home.js`. 김에 index.html의 오래된 TODO 주석
("챗봇을 팝업으로 바꿀 수도 있음")도 삭제 — 이미 `chat-widget.js`로 구현 완료된
항목이라 더 이상 할 일이 아니다.

**검증**: 탭 클릭 시 해당 단계로 정확히 전환되는 것을 확인. 처음엔 브라우저가
`home.js`를 옛 버전(1649바이트)으로 캐싱해서 안 되는 것처럼 보였는데, 캐시를
무효화한 뒤(2300바이트, 새 로직 포함) 같은 로직을 라이브 DOM에 재현해 정상
동작함을 확인 — 실제 버그 아니었음.

## 2026-08-27 (46) — 일반(밝은) 모드 재구현(교수님 피드백)

사용자 요청("선생님이 웹페이지 관련 피드백을 주셨어 ... 라이트모드") — 이전 세션에서
한 번 만들었다가 사용자 요청으로 전체 되돌렸던 다크/일반 모드 토글을, 그때 확정했던
"어둡게 낮춘" 팔레트 그대로 다시 구현. `web/css/style.css`의 `:root[data-theme="light"]`
블록과 `.theme-toggle` 버튼 스타일, `web/js/nav.js`의 테마 적용/전환 로직을 복원했다.
그 사이 다른 세션(로봇 트랙)이 `.nav-admin-link`를 기본 숨김 + `inline-flex`로 표시하는
방식으로 리팩터링해뒀길래, 그 부분은 건드리지 않고 테마 토글만 그 위에 얹었다.
실제로 토글 클릭 시 `data-theme="light"` 적용, `--bg`가 `#e7e7e3`(낮춘 밝기 값)로
바뀌는 것, `localStorage`에 저장되는 것까지 확인.

## 2026-08-27 (46) — 전체 점검 후 일괄 수정: 중계 구조 · 부저 · 안전 · UI

사용자 요청으로 웹 UI/UX·사용자기능·관리자기능·라즈봇연동·시뮬연동 5개 영역을
코드 리뷰 + 실측으로 점검했고(총 40건 발견), 그 중 코드로 고칠 수 있는 것들을 처리했다.
점검 보고서: https://claude.ai/code/artifact/5fc1a5bd-2763-407e-ba78-b3cf7307b618

### 가장 큰 것 — 로봇이 Supabase에 못 붙던 문제 (중계 구조 도입)
로봇은 **자기가 만든 핫스팟(`Raspbot`)의 AP**라서 상위 인터넷이 없다. 그래서 로봇에서
Supabase를 부르는 5개 지점이 전부 타임아웃으로 실패하고 있었다 — SR-01/SR-03 안전
이벤트, 카메라 스냅샷, 물품 조회, IP 보고. **로컬 직결 스트림만 되니까 겉보기엔
정상으로 보였다.**

로봇을 `iptime`에 붙이면 이번엔 PC가 핫스팟을 떠나야 해서 스트림·조종이 죽는다(사용자가
이미 겪음). PC만 양쪽을 동시에 본다(랜선→인터넷, Wi-Fi→로봇). 그래서:
- `event_queue.py`(신규): 로봇에 seq 커서 기반 이벤트 큐. 네트워크를 아예 안 건드린다.
- `stream_server.py`: `/events?after=<seq>`, `/events/snapshot?seq=<n>`
- `run_real.py`: Supabase 직접 호출 5곳을 전부 큐 적재로 교체
- `relay.py`(신규): PC 상주 스크립트. 큐를 비워 Supabase에 쓰고, 커서를 원자적으로
  저장하며, 영구 실패 이벤트가 큐 전체를 막지 않도록 3회 후 건너뛴다.
- `notify_supabase.py`: 중계기용 동기 버전 추가(기존 비동기판은 성공 여부와 무관하게
  True를 돌려줘서 커서 판단에 못 쓴다).

**실측 검증**: 실제 사람 감지가 로봇→큐→중계기→DB까지 도달해 `safety_events id=236,
SR-03, HIGH, NEEDS_REVIEW`로 증거사진과 함께 기록됨. SR-03이 DB에 들어간 건 처음이다.

테스트로만 잡힌 버그 2개: ① 동기 쓰기 함수가 **성공했는데 False를 반환**(이모지 print가
cp949에서 터져 except에 걸림) — 그대로 뒀으면 중계기가 같은 이벤트를 무한 재전송했다.
② 로봇이 자기 IP를 `127.0.0.1`로 보고(인터넷이 없어 라우팅 탐지 실패).

### 부저 — 아예 동작한 적이 없었음
`YB_Pcb_Car`에 `Ctrl_Buzzer`가 **존재하지 않는다**(라이브러리 직접 조사로 확인).
AttributeError가 나고 except가 삼킨 뒤에도 `/buzzer`는 성공을 반환하고 있었다.
Yahboom 공식 데모 기준으로 **BOARD 32번 핀 PWM 440Hz** 방식으로 다시 구현했고,
콜백이 없거나 실패하면 501/500을 돌려주도록 고쳤다.

**경광등(사이렌)은 폐기.** 이 보드의 LED(BOARD 38/40)는 표시등 수준이라 실내에서
경보로 인식이 안 된다는 실기기 확인 결과. 죽은 코드로 남기지 않고 전부 제거했다.

### 안전
- **Space 비상정지가 페이지당 1회만** 먹었다(keydown 목록엔 있고 keyup 목록엔 없어서
  중복 방지 가드가 이후를 전부 삼킴)
- **초음파 GPIO 동시 접근** — 제어 루프와 HTTP 스레드가 락 없이 같이 쏴서, A가 쏜 펄스를
  B가 재면 30cm 장애물이 900cm로 읽혀 그대로 들이받는다. 이제 제어 루프만 측정하고
  나머지는 캐시를 읽는다
- **후진으로 장애물 탈출 불가** → 전진일 때만 차단
- **비관리자도 키보드로 로봇 조종 가능**했다(리스너가 권한 확인 700줄 전에 등록됨)

### 거짓말하던 UI
- `lab-twin.html`이 `transitions.js`를 안 불러 **페이지 전체가 opacity:0**이었다
- "재고 실사 확정" 버튼이 성공 토스트만 띄우고 **DB에 아무것도 안 썼다**
- 카메라 배지가 프레임 도착 전에 초록불을 켜고, 한 번 끊기면 복구 경로가 없었다
- HUD가 죽은 로봇의 마지막 거리·속도를 계속 보여줬다("30 FPS"는 하드코딩)
- 부저/경광등이 실패해도 항상 성공 토스트

### 그 외
탭 전환 시 데이터 미갱신, 총 대여횟수가 취소분 포함, `zoom:1.1`이 `100vh` 계산을 깨서
로그인 화면에 74px 죽은 스크롤(→8px), XSS 이스케이프 누락 4곳, 가상실험실 CSS 변수명
불일치, 내비 링크 4개 페이지 누락, 라벨 없는 입력 23개, 좁은 화면 표 오버플로,
로그인 에러 메시지 뭉뚱그림, QR 무한 재시도, 챗봇 대화기록 유실, drive 콜백 이중 등록,
Isaac 포트 8080 충돌 무시, 시뮬 `127.0.0.1` 하드코딩, 데드맨 1초/3초 불일치.

### SQL 마이그레이션 — 실행 완료
`docs/migration_2026-08-27_relay.sql`의 6개 항목을 Supabase SQL Editor에서 전부 실행했다
(사용자가 크롬 세션으로 진행하라고 지시). 코드로 재확인:
- `robot_commands`에 `local_ip`/`cam_pan`/`cam_tilt` 컬럼 추가 → **`report_local_ip()`가
  400 실패에서 성공으로 전환**, 중계기 파이프라인이 이제 실패 0건으로 통과
- `virtual_lab_objects` 테이블 + 바인딩 10건, 확인 RPC 3개 → `PGRST202`(함수 없음)에서
  `P0001`(함수가 실행되어 잘못된 인자를 거부)로 전환 = 정상 동작
- `restock_subscriptions`/`damage_reports` UPDATE 정책, `guard_loan_self_update`
  search_path 고정, `refresh_restock_queue` 익명 실행 차단
  (호출 경로가 전부 `session.id`를 요구하는 걸 먼저 확인하고 회수했다)

### 못 한 것 (설계 결정 필요)
`record_audit_scan`은 여전히 껍데기 —
"어느 실사 세션에 속하는 스캔인지" 설계가 필요해서 임의로 정하지 않았다.
로봇 **자동순찰 기본값(`mode:"auto"`)은 일부러 안 켰다** — 사용자 부재 중에 로봇이 스스로
주행하면 물리적 사고 위험이 있어서, 옆에 있을 때 켜는 게 맞다.

## 2026-08-27 (45) — SR-03 사람 감지 안전 이벤트 + 카메라 색감 튜닝

안전 기능 로드맵 (A) 사람 감지 → (B) 물품 시각 인식 순서로 진행하기로 함
(사용자: "a쪽부터 완성하고 b쪽 가보자"). Roboflow 계정 생성은 제가 대신 할 수
없는 항목이라(계정 생성 금지), 외부 계정/모델 다운로드 없이 바로 쓸 수 있는
OpenCV 내장 HOG+SVM 보행자 검출기로 우선 구현 — 나중에 사용자가 Roboflow API
키를 넘겨주면 `real_hal.py`의 `detect_person()` 내부만 교체하면 되도록 분리해둠.

- `real_hal.py`: `detect_person()` 추가 (HOG+SVM, 현재 프레임에 사람 있으면
  True). 별도로 실물 카메라가 청록빛으로 창백하게 나오는 문제도 같이 고침 —
  Picamera2에 `AwbEnable`/`AwbMode`/`Saturation` 컨트롤 추가
  (`CAMERA_SATURATION=1.4`, `CAMERA_AWB_MODE="Auto"`, 조명 환경 따라 조정 가능).
- `run_real.py`: 메인 20Hz 틱 루프에 `PERSON_CHECK_EVERY=40`(약 2초)마다
  `check_person()`을 `db_executor`(스레드풀)로 비동기 실행 — HOG 연산이 무거워
  매 틱마다 돌리면 주행 루프가 밀리므로. 사람 감지 시 `SR-03` 안전이벤트를
  증거 스냅샷과 함께 Supabase에 전송, `PERSON_ALERT_COOLDOWN=20초`로 중복 스팸
  방지. rule_id는 설계문서 기준 SR-03(사람감지)이 맞음 — 협업 문서에 잠깐
  SR-02로 잘못 적힌 부분이 있었는데 `LabKeeper_설계문서.html`로 재확인함.

로봇 실물에 배포 후 재시작, `/health`로 스트림 FPS 정상(30fps대) 유지되는 것,
문법 에러 없는 것까지 확인. Supabase에 실제 SR-03 row가 찍히는지(사람 앞에
세워서 트리거)까지는 아직 실측 검증 전 — 다음에 확인 필요.

## 2026-08-27 (44) — 챗봇 팝업이 페이지를 넘어가도 닫기 전까지 유지되게

사용자 요청("홈화면에서 물품목록/마이페이지로 넘어갔을 때 챗봇 팝업을 내가 닫을
때까지 사라지지 않게"). 이 사이트는 SPA가 아니라 페이지마다 완전히 새로 로드되는
구조라 페이지 전환 순간의 아주 짧은 깜빡임은 없앨 수 없다고 미리 안내한 뒤 진행—
"열려있음" 여부와 드래그·리사이즈로 바꾼 위치/크기를 `sessionStorage`(탭을 닫으면
사라지는 임시 저장소)에 남겨두고, 새 페이지가 열릴 때 그 값이 있으면
`web/js/chat-widget.js`가 자동으로 다시 띄운다. 대화 내용은 원래도 서버
(`chat_messages`)에 저장되니 자동으로 이어진다. ✕ 버튼으로 닫으면 저장된 상태도
같이 지워서 그 다음부터는 페이지를 넘어가도 다시 안 뜬다. 자동으로 다시 뜰 때는
입력창에 포커스를 주지 않는다(방금 로드된 페이지에서 포커스를 뺏으면 안 눌러도
될 곳에 타이핑될 수 있어서) — 사용자가 직접 버튼을 눌러서 열 때만 포커스.

**검증**: 실제 페이지 이동(index.html → items.html → 로그인 리다이렉트 →
mobile-status.html)을 거치며 열림 상태가 유지되는 것, ✕로 닫으면 다음 페이지부터
다시 안 뜨는 것, 헤더를 드래그해서 옮긴 뒤 다른 페이지로 이동하면 정확히 그 좌표
(974px, 26.4px)로 다시 뜨는 것까지 실제 페이지 전환으로 확인.

## 2026-08-27 (43) — 로그아웃 시 챗봇 대화기록 초기화

사용자 요청("사용자별 대화 기록이 로그아웃하면 사라지게, 로그인 상태에선 페이지를
넘어가도 남아있어야 함"). 로그인 상태에서 페이지 이동해도 대화가 이어지는 건
이미 되고 있었다(`chat_messages` 테이블에 서버 저장 + 페이지 열 때마다 조회라서
애초에 페이지 자체와 무관함) — 이번에 추가한 건 "로그아웃하면 초기화" 쪽.

`web/js/chat-data.js`에 `clearChatHistory(session)` 추가 — 로그아웃하는 계정의
`chat_messages` 행을 서버에서 전부 삭제한다(RLS `chat_messages_own`이 본인 것만
지우도록 이미 막고 있어 별도 RPC 없이 일반 delete로 충분). `web/js/nav.js`의
로그아웃 클릭 핸들러에서 `signOut()`보다 먼저 호출하도록 연결 — 로그아웃 후에는
`auth.uid()`가 없어져서 RLS가 삭제 자체를 막기 때문에 순서가 중요하다. 관리자
화면(`admin.html`)엔 챗봇 FAB가 없어서 `chat-data.js`가 로드되어 있지 않았는데,
관리자도 로그아웃하면 대화기록이 지워져야 하므로 스크립트 태그를 추가했다(다른
페이지에 없을 가능성에 대비해 `nav.js`는 `window.LabBotChat` 존재 여부를 확인하고
호출).

**검증**: `window.LabBotChat.clearChatHistory`가 정상적으로 등록되는 것 확인
(브라우저 캐시가 초기엔 옛 버전을 서빙해서 처음엔 안 잡혔는데, 캐시 무효화한
스크립트로 재확인해서 실제 배포된 코드가 맞다는 것까지 확인함). 실제 로그인
계정이 없어 "로그아웃 클릭 → DB에서 진짜로 지워지는지"까지는 라이브로 검증하지
못했다.

## 2026-08-27 (42) — 챗봇 팝업 드래그 이동 + 크기 조절 추가

사용자 요청("팝업을 화면 안에서 이동가능하고, 크기조절도 가능하게 만들 수 있어?").
`web/js/chat-widget.js`에 `makeDraggable()`/`makeResizable()` 추가 — 헤더를 잡고
끌면 팝업이 움직이고, 우하단 모서리의 손잡이를 끌면 크기가 바뀐다(최소 280×320px,
화면 밖으로 못 나가게 클램프). 닫으면(그리고 다시 열면) 기본 위치/크기로 돌아간다 —
따로 저장하지 않음.

**실제로 잡은 버그 2개**:
1. 기본 CSS 높이(`min(520px, 70vh)`)가 팝업의 `bottom:156px` 오프셋을 고려하지 않아서,
   세로로 좁은 창(노트북 등)에서 팝업 위쪽(헤더 — 드래그 손잡이와 닫기 버튼이 있는
   바로 그 부분)이 화면 밖으로 밀려 올라가 안 보일 수 있었다. `height`/`max-height`
   계산에 `bottom` 오프셋을 반영해서(`calc(100vh - 168px)`) 고쳤다.
2. `setPointerCapture()`가 실패할 수 있는 API인데 감싸지 않고 호출하고 있었고,
   move/up 리스너도 작은 손잡이 엘리먼트 자체에 걸려 있었다 — 캡처가 실패하면(실제로
   합성 포인터 이벤트로 재현됨) 예외가 그대로 튀어나가 move/up 리스너 등록 자체가
   안 되고, `is-dragging`/`is-resizing` 클래스가 영영 안 지워진 채로 멈추는 상태가
   될 수 있었다. try/catch로 감싸고, 리스너를 document에 걸어서(포인터가 작은
   손잡이 밖으로 벗어나도 계속 추적) 캡처 성공 여부와 무관하게 항상 동작하도록 수정.

**검증**: 드래그·리사이즈 둘 다 pointerdown→move→up을 단계별로 발생시켜 좌표/크기가
실제로 바뀌는 것과, 마무리 후 `is-dragging`/`is-resizing` 클래스가 정상적으로
빠지는 것 확인. 닫기 버튼이 헤더 드래그에 묻히지 않고 여전히 클릭되는 것도 확인.
새 탭에서 콘솔 에러 없이 전체 흐름 재확인.

## 2026-08-27 (41) — 챗봇 FAB를 페이지 이동 대신 팝업창으로 전환

사용자 요청("페이지 이동방식보다는 그 자리 팝업방식으로 변경해주고, 팝업창을 닫을
수 있는 버튼도 만들어줘") — index.html에 있던 TODO 메모("클릭 시 챗봇 플로팅
창을 여는 방식으로 바꿀 수도 있음")를 실제로 처리.

우하단 💬 버튼이 있는 6개 페이지(index/mypage/items/mobile-status/signup/login)
전부에서 `<a href="chatbot.html">`를 `<button id="chatFabBtn">`로 바꾸고, 새
`web/js/chat-widget.js`가 클릭 시 그 자리에 작은 채팅창을 띄운다 — 메시지 이력
복원, 재고 기반 추천 카드 + 예약 버튼, gemini-chat 연동까지 `chatbot.js`와 동일한
로직을 재사용(데이터 계층은 기존 `chat-data.js` 그대로, 렌더링만 팝업용으로 새로
작성). 닫기는 헤더의 ✕ 버튼, FAB 재클릭(토글), Esc 키 셋 다 지원.

문의하기 팝업과 달리 재입고 알림·추천 카드 기능까지 필요해서, 이 6개 페이지에
없던 `items-data.js`/`rentals.js`/`chat-data.js`도 함께 추가했다(로그인/회원가입
페이지처럼 원래 재고 관련 스크립트가 전혀 없던 곳도 포함). `chatbot.html` 전체
페이지는 그대로 남겨뒀다 — 주소로 직접 들어가면 여전히 동작한다.

**검증**: 열기/닫기(✕ 버튼, FAB 토글, Esc) 4가지 전부 실제 클릭·키 입력으로
확인. 데스크톱(360px 고정폭)과 모바일(335px, `calc(100vw-40px)`) 양쪽 크기와
가로 스크롤 없음 확인. 6개 페이지 전부 콘솔 에러 없이 로드되고, mobile-status.html
포함 전부에서 팝업이 정상적으로 열리는 것까지 확인. chatbot.html 단독 페이지도
그대로 정상 로드됨.

## 2026-08-27 (40) — 대여 QR 불일치 토스트 알림 + admin.js LabBotToast.warn 미정의 버그 수정

사용자 요청("QR 다른 물품 찍으면 다른 물품입니다 이런 알림으로 나오게 그 대여하는 창에 그런것도 나오게 수정").

1. **`web/js/mypage.js`**: 대여/반납/사용 확인 모달(`openGuideModal`)에서 QR을 스캔했는데 그
   대여 건의 물품이 아닐 때(서버 RPC가 `"QR 코드가 이 물품과 일치하지 않습니다."` 예외를 던질
   때), 기존엔 모달 하단 작은 문구로만 표시되던 걸 `window.LabBotToast.error("다른
   물품입니다 — ...")`로도 함께 띄우도록 `submitCode()`의 catch 블록에 추가.
2. **`web/js/admin.js`**: 관리자 QR 스캔 실패 시 존재하지 않는 `LabBotToast.warn()`을 호출해서
   실제로는 `TypeError`가 나던 버그를 발견해 `.error()`로 수정.
3. **검증**: 로컬 정적 서버(`localhost:8765`)가 오래된 `mypage.js`를 캐싱한 채로 서빙하던
   좀비 프로세스 2개(포트 8765 중복 바인딩)를 발견해 정리하고 재기동한 뒤, 실제 대여 건
   (HPLC 시스템 반납)에 잘못된 코드를 입력해 토스트가 정상 렌더링되는 것까지 DOM 레벨로 확인.

---

## 2026-08-27 (39) — 2-트랙 0ms 직결 아키텍처 전면 개편 + 카메라 V-Sync 이중 대기 제거 + 단일 청크 원자적 write로 29.8 FPS 풀 프레임 달성

사용자 지침("우리 기본이 이제 로봇 <-> 웹 연동이고 이벤트나 저장할 내용 생겼을 때 DB 연동하는 구조로 개편 및 패치").

1. **2-트랙 직결 및 이벤트 영속성 아키텍처 전면 개편**:
   - 실시간 제어(영상 30 FPS, 조이스틱 주행, 2축 카메라 서보 각도)를 로봇 `stream_server`와 웹 간의 0ms 직결(HTTP/MJPEG)로 전면 전환.
   - 클라우드 DB는 QR 체크포인트 실사 스캔(`record_audit_scan`), 안전 이상 발생(`report_safety_event` + 현장 스냅샷 사진) 시에만 비동기로 기록.
   - 로봇 메인 루프에서 블로킹을 유발하던 Supabase 클라우드 명령 폴링을 100% 제거하여 루프 지연 0ms(20Hz 순수 하드웨어 속도) 확보.
2. **카메라 캡처 루프 이중 대기 제거 (`real_hal.py`)**:
   - `capture_array()`의 하드웨어 블로킹(33ms) 뒤에 중복으로 들어가 있던 `time.sleep(0.033)`을 제거하고 `time.sleep(0.001)` yield로 전환.
3. **MJPEG 스트리밍 서버 단일 청크 원자적 전송 (`stream_server.py`)**:
   - 개별 `send_header` 호출로 인한 TCP 패킷 파편화와 ACK 지연을 제거하고, 전체 프레임 청크를 1회 `wfile.write()`로 일괄 송출하여 **29.8 FPS 풀 프레임 달성**.
4. **웹 UI 정리 (`admin.js`, `robot-console-data.js`)**:
   - 혼란을 주던 `클라우드 스냅샷 모드` 배지와 수동 전환 버튼을 삭제하고 `🟢 실시간 직결 스트림 (10.42.0.1:8080 · 30 FPS)` 단일 상태로 직관화.
5. **실기기 원격 배포 및 검증**:
   - 라즈베리파이에 파일 전송 및 프로세스(PID 13474) 가동 검증 완료.

---

## 2026-08-27 (38) — 카메라 스트리밍 무지연 직결 고정 + 2축 팬/틸트 서보 원격 제어 파이프라인 완비 및 자동 배포

사용자 요청("최적화해서 사진 웹 연동 엄청 빨리하기로 한 방향성으로 코드 전체 리뷰 및 서보 각도 제어").
실시간 영상 송출 지연과 서보 조작 미작동 문제를 완벽 해결:

1. **실시간 MJPEG 직결 스트림 고정 (`admin.js`, `stream_server.py`)**:
   - `multipart/x-mixed-replace` 스트림 특성상 브라우저 `onload`가 발생하지 않아 매초 느린 Supabase 스냅샷으로 덮어써지던 버그를 완벽 수정.
   - 크롬/엣지의 Private Network Access(PNA) 헤더(`Access-Control-Allow-Private-Network: true`) 추가로 로컬 직결 보장.
2. **카메라 2축 팬/틸트 서보 원격 제어 파이프라인 완비 (`real_hal.py`, `stream_server.py`, `notify_supabase.py`, `run_real.py`, `robot-console-data.js`)**:
   - `real_hal.py`: `set_camera_angle(pan, tilt)` 신설로 Yahboom 2축 서보(`YB_Pcb_Car.Ctrl_Servo(1, pan)`, `Ctrl_Servo(2, tilt)`) 제어 구현.
   - `stream_server.py`: `/camera?pan=...&tilt=...` 직결 엔드포인트 신설로 십자패드 조작 시 0ms 즉각 반응.
   - `notify_supabase.py` & `run_real.py`: `cam_pan`/`cam_tilt` 필드 폴링 및 상태 동기화.
3. **원격 자동 배포 & SSH 키 인증 완성**:
   - 로봇에 `id_raspbot` 자동 인증키 등록 및 VS Code `settings.json` 연동.

---

## 2026-08-27 (37) — 새 세션 인수인계 검증 + Isaac Sim EULA 재동의 문제 해결 + run_isaac.py 문서 반영

사용자 요청("인수인계 받고 너도 이제 같이 협업하는거야" → "이거 다 수정 하고 수정내역
항상 협업 폴더에 갱신 잘 하고 깃에도 올리는거까지 해"). `labkeeper-web-local/AI협업/robot/
04_새채팅_시작문.md` 프로토콜대로 코드 수정 없이 먼저 9개 항목(GPU/스모크테스트/ROS2/
IsaacHAL/Isaac-웹 연결/RealHAL/문서-코드 차이/최소작업/AI 충돌 가능성)을 확인·보고한 뒤
승인받아 처리.

- **Isaac Sim 스모크 테스트가 Omniverse Kit EULA 재동의 프롬프트에서 멈춰 있던 문제
  발견**: `isaac_clean/smoke_test.py`, `robot-sim/isaac_project/isaac_smoke_test.py` 둘 다
  비대화형 실행 시 `Do you accept the EULA? ... EOF when reading a line`로 즉시 실패하고
  있었다(직전 08-26 성공 기록엔 이 프롬프트가 없었음 — 그 사이 Kit 캐시가 초기화된 것으로
  추정). 약관 동의 항목이라 사용자에게 먼저 물어본 뒤 승인받아 진행 — EULA 동의 후
  재실행 → `ISAAC_CONTROLLER_SMOKE_TEST_OK` 정상 통과 재확인, `isaac_clean/last_run.log`
  갱신.
- **문서-코드 불일치 발견 및 수정**: `robot-sim/isaac_project/run_isaac.py`(Isaac Sim을
  실제 창으로 띄워 `notify_supabase.py`의 `fetch_items`/`fetch_robot_command`/
  `report_safety_event`로 웹과 연동하는 진입점, 08-26 22:43 작성, git 미커밋 상태로 존재)가
  이전 인수인계 문서(`03_Claude_로봇작업결과.md`, `04_새채팅_시작문.md`,
  `05_작업내역_및_중요사항_정리.md`) 어디에도 언급이 없었음 — Isaac↔웹 연동이 "미완료"로
  남아있던 문서와 달리 코드는 이미 작성돼 있던 것. 세 문서 모두에 반영해 정합성 복구.
  실제로 실행해서 웹과 end-to-end로 확인되는지는 이번에도 검증 못함(다음 세션 과제).
- **git 상태 점검**: `git fetch` 결과 원격에 새 커밋 없음 — 다른 세션/AI와의 충돌 없음
  확인 후, 그동안(35~36번 항목 포함) 누적돼 있던 로컬 미커밋 변경사항 전부를 사용자
  승인 받아 이번에 함께 커밋·푸시함.

---

## 2026-08-27 (36) — 카메라 스트리밍 무지연 이벤트 통지 + 메모리 직접 업로드(Zero-Disk I/O) + 웹 워치독 고도화

사용자 요청("최적화해서 사진 웹 연동 엄청 빨리하기로 한 방향성으로 코드 전체 리뷰 및 최적화").
실시간 영상 송출 지연과 스냅샷 업로드 병목을 원천 해소하기 위한 전면 최적화 완료:

1. **이벤트 기반 무지연 프레임 버퍼 통지 (`stream_server.py`)**:
   - 기존 `time.sleep(0.033)` 고정 폴링을 `threading.Condition` 기반 `wait_for_new_frame(last_version)` 이벤트 대기로 개편. 새 프레임이 인코딩되는 즉시 전송 대기 클라이언트를 0ms 지연으로 깨워 30 FPS / ~30ms 초저지연 달성.
   - `get_latest_frame()` 함수를 신설하여 메모리에 보관된 최신 JPEG를 다른 모듈에 안전하게 제공.
2. **BGR888 네이티브 포맷 및 카메라 독립 루프 최적화 (`real_hal.py`)**:
   - Picamera2 설정을 `BGR888`로 최적화하여 OpenCV 인코딩 시 색상 채널 왜곡 제거 및 변환 연산 오버헤드 0화.
3. **스냅샷 메모리 직접 업로드 / Zero-Disk I/O (`notify_supabase.py`, `run_real.py`)**:
   - `upload_camera_snapshot_bytes(data: bytes)` 신설로 임시 파일 쓰기/읽기(`/tmp/labkeeper_real_camera.jpg`) 없이 메모리 바이트에서 Supabase Storage로 직접 업로드.
   - `run_real.py`에서 `hal._picam2.capture_array()`를 직접 부르던 스레드 리소스 경합을 완전히 제거하고 `stream_server.get_latest_frame()` 메모리 버퍼 활용으로 전환.
4. **웹 Robot Console 실시간 직결 + 헬스 프로브 & 자동 복구 워치독 (`admin.js`, `robot-console-data.js`, `admin.html`)**:
   - `checkStreamHealth(localIp)` 헬스 프로브 함수 추가.
   - 실시간 스트림 연결 중 로봇 오프라인 감지 시 자동 스냅샷 모드로 전환하고, 로봇 재가동 시 실시간 스트림으로 자동 복귀하는 스마트 워치독 구현.
   - Robot Console 안내 문구를 30 FPS 하이브리드 스트리밍 설명으로 최신화.

---

## 2026-08-27 (35) — 관리자 페이지에 사용자 관리 탭 추가

사용자 요청("관리자 페이지에 사용자 관리나 뭐 미납 몇번 경고 이런 기본적인 유저
관리시스템같은것도 추가해줘") — 지금까지 관리자가 사용자를 개별적으로 파악할 방법이
없었다(대여 이력, 연체 여부, 파손 신고 이력 전부 대여·파손 탭에 흩어져 있었음).

- `docs/labbot_schema.sql` 31번 섹션: `admin_list_users()` RPC 신규 — profiles에는
  이메일이 없어서(auth.users 전용) 관리자만 호출 가능한 RPC로 join해서 가져온다(관리자가
  아니면 예외). `user_warnings` 테이블 신규(관리자가 사용자에게 남기는 경고 기록 —
  safety_events와 같은 원칙으로 계정을 자동으로 막지는 않고 기록만 남김), 관리자만
  select/insert/delete 가능.
- `web/js/user-admin-data.js` 신규 — 위 RPC/테이블을 감싸는 데이터 레이어.
- `web/admin.html`/`web/js/admin.js`: "사용자 관리" 탭 추가 — 이름/이메일/권한/가입일/
  대여 이력/현재 연체/연체 이력/파손 신고/경고 횟수를 표 하나로. 재고표와 동일한
  검색·`table-wrap-wide` 패턴 재사용. "경고 추가"(사유 선택 + 기타일 때만 메모, 재고
  조정 사유 모달과 동일 UX)와 "이력"(경고 이력 확인 + 삭제) 버튼 제공. 요약 카드에
  "연체중인 사용자"(건수가 아니라 사람 수) 추가.

실행한 검증: 비관리자로 가장(SQL Editor 세션 가장)해서 `user_warnings` 직접 조회 시
실제로 존재하는 행이 있어도 0건으로 막히는 것, `admin_list_users()` RPC를 비관리자가
호출하면 예외로 거부되는 것 확인. 관리자 화면에서 실제로 경고 추가 → 목록에 "1회"
배지로 반영 → 이력 모달에서 사유·작성자·시각 확인 → 삭제까지 전체 흐름을 실제로
클릭해서 검증(테스트로 남긴 경고는 정리함). 검색창 필터링, 요약 카드의 "연체중인
사용자" 수 반영도 확인.

## 2026-08-27 (34) — 로봇 카메라 실시간 직결 MJPEG 스트리밍 + Supabase 스냅샷 Fallback 하이브리드 아키텍처 구현

사용자 지적 및 아키텍처 개선 요청("DB를 계속 거치는게 카메라 실시간성에 안 맞으니 구조 개선").
스냅샷 캡처→Supabase Storage 업로드→서명 URL 재발급→웹 폴링의 4단계 지연을 해소하기 위해 **하이브리드 실시간 직결 구조**로 개편:

1. **라즈베리파이 초경량 MJPEG 스트리머(`robot-sim/stream_server.py`) 신규 작성**:
   - 외부 무거운 웹서버 프레임워크 없이 순수 파이썬 표준 라이브러리(`http.server`, `threading`)만 사용하여 포트 8080에 `/stream` (MJPEG), `/snapshot`, `/health` 서빙.
   - `real_hal.py`의 `capture_frame()`과 연동하여 캡처 즉시 스트림 버퍼 갱신.
2. **로봇 로컬 IP 자동 공유 (`notify_supabase.py` / `docs/labbot_schema.sql`)**:
   - 로봇 부팅 및 실행 시 `get_my_local_ip()`를 통해 현재 LAN IP(예: `192.168.0.22`)를 `robot_commands.local_ip`에 기록.
3. **웹 관리자 Robot Console 실시간 직결 + Fallback (`admin.js` / `robot-console-data.js`)**:
   - 같은 로컬 네트워크일 경우 `http://<local_ip>:8080/stream`으로 직접 연결하여 **50~100ms급 초저지연 실시간 영상** 제공.
   - 외부망이거나 스트리머 미가동 시 자동으로 기존 **Supabase Storage 서명 URL 스냅샷 모드로 즉시 Fallback**되어 서비스 중단 없이 안정성 유지.
   - 일반 대여/재고/챗봇/안전관리 웹 기능은 Supabase 클라우드에 유지되어 로봇 전원 상태와 무관하게 24시간 가동 보장.

---

## 2026-08-26 (33) — real_hal.py 구현 완료 + Raspbot 정품 이미지 재설치 + Wi-Fi 전환 착수

`robot-sim/real_hal.py`가 그동안 `NotImplementedError`만 던지는 자리였는데, 실제 로봇에
Raspberry Pi Connect Remote shell / Jupyter Lab으로 직접 접속해서 로봇에 설치된 Yahboom
공식 데모 노트북(`~/Yahboom_project/Raspbot/...`)의 GPIO 핀·라이브러리 호출을 그대로 읽어
구현했다(추측 없음, 출처는 `real_hal.py` docstring에 남김): 초음파 Trig=16/Echo=18(BOARD),
라인트래킹 4채널(BOARD 11/7/13/15), 모터 `YB_Pcb_Car.Control_Car()`, QR
picamera2+pyzbar. `controller.py`는 한 글자도 안 고침(HAL 5메서드 시그니처 유지).
`robot-sim/run_real.py`(신규)를 `labkeeper_controller.py`(Webots)와 같은 구조로 작성 —
dead-man switch, 수동/자동 모드, Supabase 체크포인트, 카메라 업로드, JSONL 텔레메트리.
둘 다 아직 로봇엔 안 올렸고 git에도 커밋 안 함(사용자 요청 시에만 커밋).

이 과정에서 이전에 재구운 SD카드가 **일반 Raspberry Pi OS(Trixie)**였고 Yahboom 라이브러리가
전혀 없었다는 걸 로봇 셸에서 직접 재확인(`find`로 yahboom 관련 파일 0개) — Yahboom 공식
Google Drive(`yahboomsmart01` 계정)에서 `Raspbot_PI5_20240425.zip`(6.85GB)을 받아 정품
이미지로 재설치. 이 이미지는 Raspberry Pi Imager의 자동 Wi-Fi/SSH 주입이 안 먹히고 대신
로봇 자체 핫스팟을 켜는 게 기본 동작이라, 핫스팟(`Raspbot`)에 붙여 Jupyter Lab으로 들어가서
`sudo nmcli`로 `iptime`(집 공유기) 연결 프로필을 만들고 우선순위를 자체 핫스팟보다 높여
전환을 시작했다. 전환 후 로봇 자체 핫스팟이 꺼지는 건 정상 동작이지만, PC의 Wi-Fi가 현재
위치에서 `iptime` 신호가 약해(RSSI 255) 실제 전환 성공 여부는 이 세션에서 확인 못 함 —
다음 세션에서 폰이나 공유기 관리자 페이지로 확인 필요. 김에 `git fetch`로 web/ 쪽 안 받아온
커밋 2개(메인화면 변경/수정)를 발견해 `--ff-only`로 반영하고, 로컬 정적 서버로 콘솔 에러
없이 뜨는 것까지 확인함. 상세 경과는
`labkeeper-web-local/AI협업/robot/03_Claude_로봇작업결과.md` 참고.

## 2026-08-26 (32) — 홈 화면 자율 디자인 패스: 이용방법 아이콘 SVG 통일 + 푸터 추가

사용자 요청("너 마음대로 지금 느낌 살려서 디자인 추가하고 싶은 부분과 메뉴 추가라던지
마음대로 바꿔봐"). 자체 디자인 점검에서 여러 번 지적된 "SVG 아이콘과 이모지 혼용"
문제를 이 페이지에서라도 해소하고, 로고 하단이 허전했던 홈 화면에 푸터를 새로
추가. 리스크가 큰 변경(다른 페이지 전역 수정, 인증/데이터 흐름 변경)은 피하고
index.html 안에서 끝나는 시각적 다듬기로 범위를 좁혔다.

**이용방법 카드 아이콘 이모지 → SVG로 교체** — 📦🤖📷🔁 4종을 로고/관리자 링크
아이콘과 같은 라인아트 스타일(24x24, `stroke="currentColor"`, stroke-width 1.6)로
새로 그려 넣었다(`web/index.html`, `web/css/style.css`의 `.step-icon`). 대여/반납
토글로 아이콘이 숨겨졌다 나타나는 기존 동작에 영향 없는 것까지 확인함.

**홈 화면 전용 푸터 추가** — 로고 + 한 줄 소개, 초록 펄스 점 + 실시간 시계(브라우저
로컬 시각, 1초마다 갱신)로 구성(`web/index.html`, `web/css/style.css`,
`web/js/home.js`). 모바일 폭(375px)에서 가로 스크롤 발생 안 하는 것 확인. 처음엔
바로가기 링크(물품목록/마이페이지/챗봇)와 시계 앞에 "SYSTEM ONLINE ·" 문구도
넣었는데, 사용자 요청으로 둘 다 빼고 로고+소개+시계만 남겼다.

## 2026-08-26 (31) — 홈 화면 히어로 안내 문구 삭제(자리는 공란으로 유지)

사용자 요청("물품목록에서 원하는 장비..." 내용을 삭제해주고 그 공간만큼 공란으로
만들어줘"). 먼저 라즈봇 애니메이션으로 교체해봤다가 사용자가 원복을 요청해서
되돌렸고, 이번엔 문구만 비우고 자리는 남기는 쪽으로 확정. `.hero-desc` 문단의
텍스트를 지우고, 두 줄 문구가 차지하던 만큼의 세로 공간이 갑자기 좁아지지 않도록
`min-height: 2.7em`을 추가(`web/index.html`, `web/css/style.css`). 실제로 빈
문단의 높이가 46.6px로 유지되는 것과 텍스트가 완전히 비어있는 것을 확인함.

## 2026-08-26 (30) — 관리자 메뉴를 관리자로 로그인했을 때만 노출

사용자 요청("메인페이지에서도 관리자 메뉴를 삭제해주고 관리자로 로그인 했을때
메뉴가 생기게"). 이전엔 "로그인 전에도 admin.html로 들어가야 한다"는 이유로
비로그인 상태에서도 상단 네비 "관리자" 링크를 노출했는데, 바로 앞 커밋(29번)에서
admin.html이 `requireLogin()`으로 바뀌어 링크가 없어도 주소를 직접 입력하면
로그인 페이지로 안내되고 로그인 후 자동으로 돌아오므로, 이 예외가 더 이상 필요
없어졌다. `web/js/nav.js`의 조건을 `session && session.role === "admin"`일 때만
보이도록 변경 — 비로그인/일반 사용자에게는 완전히 숨겨지고, 관리자로 로그인했을
때만 나타난다. 새 탭에서 비로그인 상태로 직접 확인(display:none).

## 2026-08-26 (29) — 관리자 로그인 폼 중복 제거, 로그인은 login.html 한 곳으로 통일

사용자 질문("관리자든 일반사용자든 로그인하는곳은 똑같지?")에 답하다가 실제로는
admin.html 안에 완전히 별도인 로그인 폼("관리자 로그인 게이트")이 하나 더 있다는
걸 확인 — login.html과 코드가 중복이었고, 관리자가 아닌 계정으로 그 폼에 로그인을
시도하면 조용히 로그아웃까지 시켜버려서("관리자 계정 정보가 올바르지 않습니다"라는
부정확한 에러와 함께) 혼란스러운 경험이었다. 사용자 요청("로그인은 동일한곳에서
진행하고 관리자 카테고리와 관리자 페이지는 관리자만 볼 수 있게")에 따라 정리.

admin.html의 자체 로그인 폼(`adminGate`)을 제거하고, items.html/mypage.html과 같은
`requireLogin()` 패턴으로 교체 — 비로그인 상태면 `login.html?redirect=admin.html`로
보내고, 로그인 성공 후 자동으로 admin.html로 돌아온다(`auth.js`의 REDIRECT_ALLOWLIST에
admin.html이 이미 등록돼 있어 추가 작업 없이 그대로 동작). 로그인은 했지만 관리자가
아닌 경우를 위한 "권한 없음" 안내 화면(`adminForbidden`)을 새로 추가 — 예전처럼
로그아웃시키지 않고, 홈으로 돌아가는 링크만 보여준다. `web/admin.html`(로그인 폼 →
권한없음 안내로 교체), `web/js/admin.js`(gate/loginForm 관련 코드 전부 제거,
`requireLogin` 기반으로 재작성).

## 2026-08-26 (28) — 자체 디자인 점검: 물품목록 재입고 알림 버튼 모바일 오버플로 수정 외 2건

사용자 요청("디자인적으로 부족한 부분을 찾아서 말해줘")으로 재입고 알림 기능을 다시
점검하고 실제 375px 뷰포트에 렌더링해서 좌표까지 찍어 검증한 3건 중 2건은 즉시
수정, 1건은 확인 후 수정.

**물품목록 "재입고 알림 신청" 버튼이 모바일에서 카드 밖으로 넘침 (확정, 수정함)** —
`.item-row-meta`가 `flex-shrink: 0`이라 줄어들지 않는데, 새 버튼 문구(8글자, ~143px)가
이 슬롯에 있던 기존 문구들("사용하기"/"예약하기"/"재고없음", 전부 4글자·~65px)보다
2배 이상 길어서 375px 폭에서 카드 우측 경계를 14px 넘어갔다(실측: 카드 끝 353px,
버튼 끝 367px). "재입고 알림 신청" → "재입고 알림"으로 문구를 줄여 해결
(`web/js/items.js`) — 수정 후 재측정하니 메타 영역이 카드 안(332px)에 들어옴.

**마이페이지 재입고 목록의 "신청 취소" 버튼 위치가 카드마다 달랐음 (확정, 수정함,
이전 턴에서 처리)** — "지금 예약 가능"(버튼 2개) 카드와 "대기 중"(버튼 1개) 카드가
섞이면 취소 버튼 x좌표가 최대 99px 어긋났다. "대기 중" 카드에 보이지 않는 자리채우기
버튼을 넣어 항상 같은 위치(141.5px)에 오도록 수정(`web/js/mypage.js`).

**"재입고 알림 신청"/"알림 신청됨" 버튼 색이 똑같았음 (확정, 수정함, 이전 턴에서
처리)** — `.category-filter-btn.active`와 같은 강조색 패턴을 재사용해 신청 상태를
`.is-subscribed` 클래스로 시각 구분(`web/css/style.css`, `web/js/items.js`).

**확인했지만 문제 아니었던 것**: 마이페이지 배지 텍스트("지금 예약 가능 · N시간
M분 남음")가 좁은 화면에서 카드 밖으로 넘칠 거라 의심했으나, 실측 결과 배지 폭이
카드 폭 안에 들어와 문제 없었다(괜히 안 고침).

## 2026-08-26 (27) — 재입고 알림 목록에서 바로 예약 가능하게

사용자 요청("마이페이지 재입고 알림 신청 에서도 재입고가 되었을때 예약하기가
가능하게 만들어줘"). 우선권을 쥔 상태(지금 예약 가능)인 신청 건에만
"예약하기/사용하기" 버튼을 추가 — items.html까지 다시 가지 않아도 마이페이지에서
바로 예약할 수 있다. 기존 reserveItem()을 그대로 호출하므로 재고 확인·재입고
대기열 정리(leaveRestockQueue)·로봇 수령 흐름까지 items.html에서 예약할 때와
완전히 동일하게 동작한다. 예약 성공 시 "수령 대기중인 예약" 목록도 함께 새로고침.
`web/js/restock-data.js`의 `fetchMySubscriptions()`가 물품의 `item_type`/`available_qty`도
같이 가져오도록 확장(`reserveItem`/`isConsumable`에 필요).

## 2026-08-26 (26) — 마이페이지에 재입고 알림 신청 목록 추가

사용자 지적("마이페이지에는 알림신청 목록은 없어도 되는건가?") — 신청/취소가
items.html 물품 행 버튼에서만 가능해서, 어디에 뭘 신청해뒀는지 한눈에 볼 곳이
없었다. "내 문의"와 같은 자리에 "재입고 알림 신청" 섹션을 추가(`web/mypage.html`,
대여중인 물품과 내 문의 사이) — 신청 목록을 물품명 + 상태(대기 중 / 지금 예약 가능·
남은 시간)로 보여주고, 여기서도 바로 취소할 수 있다. `web/js/restock-data.js`에
`fetchMySubscriptions()` 추가, `web/js/mypage.js`에 렌더링/취소 로직 추가.

## 2026-08-26 (25) — 재입고 알림을 신청 순서 기반 우선순위 대기열로 확장

사용자 요청("알림신청을 하면 자동예약되고 그 순서에 맞게 우선순위를 지정. 재입고 후
8시간 안에 예약신청을 하지 않으면 다음 우선순위에게 넘어감. 알림취소를 누르면 취소
및 다음우선순위에게 넘어감"). 요구사항 중 "자동예약"의 의미가 뒷문장("8시간 안에
예약신청을 하지 않으면")과 앞뒤가 안 맞아 먼저 확인 — **우선권만 자동으로 부여**하고
실제 예약(loans)은 여전히 사용자가 직접 눌러야 하는 방식으로 확정(클릭 없이 로봇
수령 흐름을 강제로 시작시키는 완전 자동예약은 채택하지 않음). "8시간 경과" 판정도
이 프로젝트에 cron이 없어서 실시간 타이머가 아니라 페이지 방문 시점 지연 판정으로
확정 — 둘 다 AskUserQuestion으로 확인 후 진행.

`restock_subscriptions`에 `hold_expires_at`/`notified_at` 컬럼 추가 + `refresh_restock_queue()`
security definer RPC 신설(`docs/labbot_schema.sql` 30번 섹션) — 우선권 8시간 경과분을
삭제(다음 순위로 자동 이동)하고, 재고 있는 물품 중 현재 우선권자가 없으면 가장 오래
기다린 신청자에게 8시간 우선권을 새로 부여한다. 신청/취소/예약 시점마다
`refreshRestockQueue()`를 호출해 그때그때 지연 반영(`web/js/restock-data.js`).
예약(`reserveItem`)에 성공하면 대기열에서 자동으로 빠지고 남은 재고가 있으면 다음
순위가 바로 승격되도록 `web/js/rentals.js`에도 연결.

**한계 — 우선권은 재고를 실제로 잠그지 않는다.** 대기열에 없는 다른 사용자가 물품목록에서
그 사이 먼저 예약해버리는 것까지 막지는 못한다(우선권자에게만 먼저 알려준다는 의미).
실제 재고를 예약 대상자용으로 홀드하려면 available_qty 예약 로직 자체를 바꿔야 해서
이번 범위에는 포함하지 않았다.

**아직 라이브 DB로 전체 흐름(신청→재입고→우선권 부여→미예약 시 다음 순위 이동)을
확인하지 못했다** — 이 환경에 테스트 계정이 없어 모듈 로드/문법 검증까지만 했다.
`docs/labbot_schema.sql` 30번 섹션도 Supabase SQL Editor에서 먼저 실행해야 한다
(29번 섹션이 이미 적용되어 있어야 함 — `alter table ... add column if not exists`라
순서만 지키면 여러 번 실행해도 안전).

## 2026-08-26 (24) — 품절 물품 재입고 알림 신청 기능 추가

사용자 요청("대여물품 품절시 재고가 들어오면 예약했던 사람에게 알림을 보내야하는데
어떤 방식이 좋을까" → "알림신청 기능으로 추가해줘"). 품절(OUT_OF_STOCK)된 물품의
"재고없음/대여불가" 버튼 자리에 "재입고 알림 신청" 토글 버튼을 추가(장비/PPE의
"대여불가"·소모품의 "재고없음"과 달리, 품절만 재고가 다시 들어오면 해결되는
상태라서 이 상태에서만 노출). 신청해두면 재고가 다시 0보다 커졌을 때 다음 로그인·
페이지 방문 시 토스트로 알려준다.

새 `restock_subscriptions` 테이블(`docs/labbot_schema.sql` 29번 섹션) — item_id/user_id
유니크 제약으로 중복 신청 방지, RLS로 본인 행만 보고/걸고/지울 수 있다. inquiries와
달리 RPC 없이 단순 CRUD로 충분(원자적으로 묶어야 할 여러 컬럼 갱신이 없어서).
알림을 띄운 신청 건은 그 자리에서 서버 행을 삭제하는 방식이라, 이전에 지적했던
"답변완료 알림이 localStorage 기반이라 다른 브라우저/기기에서 또 뜨는" 문제가
여기서는 애초에 생기지 않는다(`web/js/nav.js`의 `notifyRestockedItems`).

`web/js/restock-data.js`(신규) — subscribeRestock/unsubscribeRestock/
fetchMySubscribedItemIds/fetchReadyRestockNotifications/consumeRestockNotification.
`web/js/items.js` — 품절 물품 행에 신청/신청됨 토글 버튼과 클릭 핸들러 추가,
페이지 진입 시 내 신청 목록을 한 번에 불러와 N+1 조회 방지. 모든 페이지의 nav.js
로드 지점에 `restock-data.js` 스크립트 태그 추가.

**아직 실제 로그인 세션으로 신청→관리자 재고 조정→토스트까지 전체 흐름을 라이브
DB에서 확인하지 못했다** — 이 환경에 테스트 계정이 없어 모듈 로드(콘솔 에러 없음,
`window.LabBotRestock` 함수 5개 노출)와 `node --check` 문법 검증까지만 했다. 실제
DB에 `restock_subscriptions` 테이블도 아직 안 만들어져 있으니, `docs/labbot_schema.sql`
29번 섹션을 Supabase SQL Editor에서 먼저 실행해야 동작한다.

## 2026-08-26 (23) — 문의하기 기능 자체 디자인 점검 반영 (종결 상태 살림 + 상세 다듬기 7건)

사용자 요청("디자인/디테일 부족한 점 봐줘" → "전부 진행해줘")으로 27번 섹션에서 만든
"관리자에게 문의하기" 기능을 스스로 다시 훑어보고 나온 문제 7가지를 전부 반영.

**종결(closed) 상태가 코드상 도달 불가능했던 죽은 값** — `reply_inquiry()`가 무조건
`answered`로만 바꾸고, 상태를 `closed`로 옮기는 버튼이나 로직이 어디에도 없었다(스키마
`status` 컬럼에만 정의된 값). `close_inquiry()` RPC를 새로 추가(28번 섹션, reply_inquiry와
같은 원자적 갱신 원칙)하고, 관리자 카드에 "종결" 버튼을 붙였다 — `answered` 상태에서만
누를 수 있고, `closed`가 되면 답변을 읽기 전용으로만 보여준다(`web/js/admin.js`,
`web/js/inquiry-data.js`).

**답변완료 문의도 무방비로 덮어쓰기 가능했던 문제** — 상태와 무관하게 항상 "답변 등록"
버튼 하나만 있어서, 이미 답변한 문의를 실수로 다시 눌러도 확인 없이 조용히 덮어씌워졌다.
`answered` 상태에서는 버튼 문구를 "답변 수정"으로 바꾸고, 등록 전 `confirm()`으로 한 번
더 확인받도록 수정.

**"답변대기" 배지 색이 중립 회색이라 정작 처리해야 할 상태가 안 눈에 띔** — 요약 카드는
이미 `summary-card-warn`(앰버)로 강조하면서 목록 배지는 `badge-pending`(회색, damage
분석중 개념을 그대로 재사용한 것)을 써서 같은 상태를 서로 다르게 표현하고 있었다.
`safety_events`에 이미 있던 `badge-st-needs_review`(앰버 + 펄스)로 통일
(`web/js/inquiry-data.js`).

**문의 목록에 검색/필터/페이지네이션이 전혀 없었음** — 재고 테이블·물품목록과 달리
`fetchAllInquiries()` 결과를 통째로 렌더링했다. items.js와 같은 방식(`PAGE_SIZE`, 이전/다음
버튼)으로 관리자 화면(`web/js/admin.js`)과 마이페이지(`web/js/mypage.js`) 양쪽에
페이지네이션 추가, `admin.html`/`mypage.html`에 `#inquiryPagination` 컨테이너 추가.

**문의 작성 모달에 포커스 이동/ESC 닫기가 없었음** — 모달을 열 때 제목 입력창에 자동
포커스를 주고, `Escape` 키로도 닫히도록 `web/js/inquiry-widget.js`에 추가.

**제출/답변 버튼에 진행 상태 표시가 없었음** — 클릭 시 `disabled`만 걸릴 뿐 문구가
그대로여서 느린 네트워크에서는 버튼이 죽은 것처럼 보였다. "전송 중..."/"등록 중..."으로
바뀌었다가 실패 시 원래 문구로 되돌아가도록 수정.

**모바일 화면에서 토스트 알림이 챗봇/문의하기 플로팅 버튼과 겹칠 수 있었음** — 480px 이하에서
토스트 컨테이너가 화면 하단 전체 폭(`bottom:16px`)을 차지하는데, 두 플로팅 버튼이 쌓인
윗변(`bottom:88px + 56px = 144px`)과 겹쳤다. 이번에 추가한 "답변완료 알림"(`nav.js`)으로
토스트가 뜨는 빈도가 늘어난 참이라, 토스트 하단 여백을 `156px`로 올려 버튼 위에 뜨도록
수정(`web/css/style.css`).

## 2026-08-26 (22) — GPT Bugbot+Security 심층 리뷰 반영 (QR 비공개화 등 P0 4건 + P2 3건)

사용자 지시("지금 최신리뷰 기반으로 수정 ㄱㄱ") — `01_GPT_최신리뷰.md`에 새로 추가된
"10. Bugbot + Security 코드 리뷰" 섹션의 P0/P1/P2 항목을 반영. 로봇 실기기가 필요한
P0-5(라즈베리파이 service-role 키)만 로봇 트랙 소관이라 제외.

**P0-1 (QR 코드가 로그인만 하면 누구나 조회 가능했음)** — `items_select_all`이
`qr_code`까지 그대로 노출해서, 실제로 QR을 스캔하지 않고도 콘솔에서
`supabaseClient.from('items').select('qr_code')`로 값을 가져와 확인 RPC를 호출할 수
있었다("물리적으로 QR을 찍어야만 확인된다"는 프로젝트 핵심 전제가 깨지는 구멍).
`qr_code`를 `item_qr_codes` 별도 테이블로 분리하고 관리자만 조회 가능하게 막음
(`docs/labbot_schema.sql` 24번 섹션). `confirm_loan_pickup`/`confirm_loan_return`/
`confirm_item_usage`를 `security invoker` → `security definer`로 전환(item_qr_codes는
관리자만 읽을 수 있어서 invoker로는 이 RPC들 자체가 항상 실패하게 됨).
`web/js/items-data.js`(`item_qr_codes(qr_code)` embed + `qrCodeOf()` 헬퍼),
`web/js/rentals.js`(LOAN_SELECT에서 qr_code 제거), `web/js/admin.js`(재고표 QR
표시를 `qrCodeOf()`로 교체)도 함께 수정.

**P0-2 (loans 직접 INSERT로 예약 단계 자체를 건너뛸 수 있었음)** — `loans_insert_own`
정책이 `user_id`만 확인해서, 일반 사용자가 `status='대여중'`/`'반납완료'`를 직접
INSERT하면 예약과 QR 확인을 통째로 건너뛸 수 있었다(재고 트리거는 그대로 작동해서
겉보기엔 정상처럼 보여 더 위험했음). 새 `guard_loan_self_insert` 트리거로 일반
사용자의 INSERT는 `예약중` 상태 + 진행 필드 전부 NULL일 때만 허용(25번 섹션).

**P0-3 (소모품 수량 확정 시 권한 오류 가능성, Bugbot 지적)** — `confirm_item_usage`가
`security invoker`인 채로 `items` UPDATE를 실행해서, 일반 사용자가 수량 1개가 아닌
사용을 확정할 때마다 "items 쓰기는 관리자만" RLS에 걸려 실패할 수 있었다. 위 P0-1
작업에서 함께 definer로 전환하며 같이 해결.

**P0-4 (파손 AI 신고 분석 API가 소유권을 검증하지 않음)** — `gemini-damage-assess`
Edge Function이 Authorization 헤더가 "있는지"만 확인하고 실제 JWT를 검증하지 않아서,
임의의 `report_id`로 다른 사용자의 파손 신고를 분석 요청할 수 있었다(분석 결과로
사진 내용을 간접적으로 알아낼 수 있음). Supabase Auth `/auth/v1/user`로 실제 로그인
세션인지 검증하고, 신고 당사자 또는 관리자만 통과하도록 수정. 대시보드에서 배포까지
완료(`supabase functions deploy`가 아니라 Editor 통해 배포하는 이 프로젝트 방식 그대로).

**P2 보완 3건**: `is_admin()`에 빠져 있던 `set search_path = public` 추가(다른 definer
함수들과 통일), `damage-photos` 업로드 정책에 조회 정책과 동일한 "업로드 경로 첫
폴더 = 본인 UID" 조건 추가(다른 사용자 폴더에 사진을 올릴 수 있던 문제),
`cancel_loan_reservation()`이 무조건 `+1`하던 것을 `least(available_qty+1, total_qty)`로
캡(관리자가 예약 중에 total_qty를 낮추면 취소가 제약 위반으로 실패할 수 있던 문제).
그 외 `web/js/rentals.js`의 미사용 우회 함수 `consumeItem()`/`returnLoan()`(QR 확인
없이 대여를 끝낼 수 있던 죽은 코드) 제거, `docs/labbot_seed_items_v3_grad_expansion.sql`에
재실행 시 중복 방지 가드 추가.

실행한 검증(전부 실제 프로덕션 DB에서):
- 비관리자로 가장(SQL Editor `set role authenticated` + 가짜 JWT)해서 `item_qr_codes`
  직접 조회 → 0행(P0-1 확인), `status='대여중'`으로 직접 INSERT 시도 → 트리거가 정상
  차단(P0-2 확인)
- 같은 비관리자로 정상적인 `예약중` INSERT는 성공, 그 예약에 `confirm_item_usage(qty=2)`
  호출 → 정상 처리되고 재고가 정확히 반영됨(P0-3이 실제로 고쳐졌는지 확인 — 이전
  정의였다면 이 호출은 권한 오류로 실패했어야 함)
- 관리자 세션으로 예약→틀린 QR 거부→올바른 QR 픽업→반납까지 전체 사이클 재확인,
  재고 원복까지 확인
- 관리자 재고관리 화면 QR 썸네일·다운로드 정상 렌더링, 물품목록/마이페이지/관리자
  하드리로드 후 콘솔 에러 없음 확인
- `gemini-damage-assess`를 가짜 토큰으로 호출 → 401 확인(다만 실제 "다른 사용자
  소유 신고를 자기 계정으로 시도" 케이스는 로그인 가능한 두 번째 테스트 계정이 없어
  실제 호출까지는 재현하지 못함 — 코드 리뷰 + 배포 성공까지만 확인)

**이번에도 다루지 않음**: P0-5(라즈베리파이의 service-role 키 — 로봇 실기기 접근이
필요해서 로봇 트랙 담당, 아래 참고), P3(Gemini 챗봇 비용 방어용 요청 제한 — 발표
전 긴급하지 않다고 판단해 보류).

## 2026-08-26 (21) — 유효기간 시연용 물품 2종 복구 (DB 데이터만, 코드 변경 없음)

사용자 질문("99종이면 충분해?")에 답하다가 실제 DB 상태를 점검했는데, "유효기간 임박"·
"유효기간 만료" 배지가 하나도 안 뜨는 것을 발견. v2 시딩 당시 이 두 상태를 보여주려고
일부러 만들어뒀던 "DNA ladder"(임박용)/"PCR master mix"(만료용)의 `expires_at`이 어느
시점에 null로 비어 있었음(메모는 "발표용: 유효기간 임박/만료 시연"으로 그대로 남아 있어서
원래 의도는 확인 가능했음).

- `items.id=56`(DNA ladder) `expires_at`을 오늘로부터 +20일로 복구 → "유효기간 임박" 배지
- `items.id=57`(PCR master mix) `expires_at`을 오늘로부터 -10일로 복구 → "유효기간 만료" 배지
- 코드 변경 없음(순수 DB 데이터 수정, 관리자 세션에서 직접 UPDATE) — 이 항목은 커밋 대상
  파일이 없어 별도 커밋 없음.

실행한 검증: 관리자 재고관리 화면에서 두 물품 모두 의도한 배지("유효기간 임박 · 대여
가능", "유효기간 만료")가 뜨는 것을 직접 확인.

## 2026-08-26 (20) — 물품 목록을 대학원 실험실 규모로 확장 (38종 추가)

사용자 요청("물품같은거 실제 실험실 기반에서 좀 더 확장해서 대학원 실험실정도에 물품
목록으로 디비에 넣어줘") — 기존 61종(v2, 학부 실습실 수준의 기본 분자생물학/세포배양
물품)이 다소 단순해서, 대학원 랩에 실제로 있지만 빠져 있던 항목을 추가.

- `docs/labbot_seed_items_v3_grad_expansion.sql` 신규 — 기존 물품은 하나도 지우거나
  바꾸지 않고(v2와 달리 DELETE 없음) 38종만 INSERT:
  - 장비(EQUIPMENT) 10종: 초저온 냉동고(-80℃), -20℃ 냉동고, 4℃ 냉장고, 초음파 파쇄기,
    Western blot 전기영동 장치, 세미드라이 트랜스퍼 장치, 화학발광 이미징 시스템,
    진탕 배양기, 자동 세포계수기, 액체질소 탱크
  - 시약(REAGENT) 12종: 항생제(Ampicillin/Kanamycin), LB broth/agar, 컴피턴트 세포,
    제한효소 세트, T4 DNA ligase, Bradford 키트, ECL 발광 기질, BSA, 에탄올, 이소프로판올
  - 소모품(CONSUMABLE) 10종: 실린지 필터 2종, PVDF membrane, Parafilm, cell strainer,
    cell scraper, weighing paper, SDS-PAGE precast gel, 6/96-well plate
  - PPE 3종, 안전물품(SAFETY) 3종: 방진마스크, 화학용 앞치마, 라텍스프리 장갑, 소화기,
    방독마스크, 화상 처치 키트
  - 기존 9개 고정 위치(로봇 체크포인트)와 기존 5개 카테고리 체계를 그대로 재사용 —
    새 위치·카테고리를 만들지 않아서 로봇 체크포인트나 필터 UI를 따로 손볼 필요 없음.
  - qr_code는 기존 물품과 동일하게 DB 트리거(`items_set_qr_code`)가 INSERT 시 자동 발급.
- 실제 DB에는 관리자 세션의 `supabaseClient.from('items').insert(...)`로 반영(파일의
  SQL과 값은 동일 — 재현용 문서로 파일도 함께 남김).

실행한 검증: 삽입 후 총 물품 수 61 → **99**, 카테고리별 개수(장비10/시약12/소모품10/
PPE3/안전물품3)가 의도한 값과 정확히 일치 확인. 관리자 재고관리 화면에서 새 항목이
정상 렌더링되고 QR 코드가 발급된 것, 검색창에 새 물품명("초저온")을 입력하면 정상
필터링되는 것까지 실제 화면에서 확인.

## 2026-08-26 (19) — 일반 사용자에게 관리자 링크 숨김

사용자 지적("일반사용자로 로그인하면 관리자 버튼은 사라지게 해줘 로그아웃만 있고") —
상단 네비게이션의 "관리자" 링크가 로그인한 일반 사용자에게도 그대로 보이던 것을 수정.

- `web/js/nav.js`: `session.role !== "admin"`이면(=로그인한 일반 사용자) `.nav-admin-link`를
  `display:none`. 비로그인 상태(관리자가 로그인 전 화면에서도 `admin.html`로 들어갈 수
  있어야 함)와 관리자 로그인 상태는 그대로 노출. 모든 페이지가 같은 `nav.js`를 공유해서
  파일 하나만 고치면 전체 반영됨.
- 이 링크 숨김은 UI 편의 기능일 뿐, 실제 접근 차단은 여전히 DB의 `is_admin()` RLS가
  담당(주석으로 명시). 즉 URL을 직접 입력해 `admin.html`에 들어가더라도 데이터 접근은
  기존처럼 RLS가 막는다.

검증: 실제 페이지의 `.nav-admin-link` DOM에 nav.js와 동일한 로직을 그대로 실행해서
일반 사용자 role → 숨김, 관리자 role → 노출, 비로그인 → 노출 세 가지 케이스 모두
의도대로 동작하는 것을 확인.

## 2026-08-26 (18) — GPT 리뷰 P3 다듬기 항목 반영 (실기기 로봇 검증 제외)

사용자 지적("로봇 실무하는거 빼고 수정할거 다 수정하고") — 17번에서 남겨둔 P3 항목 중
실제 로봇/실기기가 필요 없는 것들을 마저 반영.

- **Robot Console 설명문이 작고 길어 핵심이 묻힘**: "3초 명령 타임아웃"과 "장애물 앞
  강제정지"를 별도 줄로 분리해 굵은 글씨 + 일반 크기(0.88rem)로 먼저 보여주고, 나머지
  부가 설명만 기존처럼 작은 회색(mono) 글씨로 아래에 남김. `web/admin.html`,
  `web/css/style.css`(`.robot-console-key-note` 신규).
- **한/영 용어 혼재**: 관리자 요약 카드가 다른 카드들("재고 부족 · 품절", "파손 신고
  확인 필요" 등)과 다르게 유일하게 "Safety 검토 필요"라고 영문 표기 — 탭 이름("안전
  이벤트")과도 안 맞아서 "안전 이벤트 검토 필요"로 통일. "Robot Console"/"Webots"/
  "Raspbot"/"PPE"는 고유명사(제품명·표준약어)라 그대로 유지가 맞다고 판단해 손대지
  않음.
- **관리자 재고표 모바일 카드 변환 CSS 재확인**: 기존에 이미 만들어져 있던
  `@media (max-width: 860px)` 표→카드 변환 규칙(`data-label` 기반)을 다시 읽어서
  구조적으로 맞게 되어 있는 것과, 이번에 추가한 검색/필터 UI(`.list-toolbar`,
  물품목록과 동일 컴포넌트라 이미 반응형)가 이 규칙을 깨지 않는 것을 코드로 확인.
  단, 실제 모바일 기기로 로그인한 상태에서 눈으로 보는 검증은 관리자 데모 계정
  로그인 정보가 없어 이번에도 못 함(이전 라운드부터 반복된 제약 — 아래 "사용자가
  직접 해야 할 작업" 참고).

**이번에도 반영하지 않음(로봇 실물/실기기가 필요한 항목 — 사용자 지시대로 제외)**:
P1-3(실제 카메라로 QR 스캔 리허설), GPT 리뷰 9번 섹션의 로봇 기능 추천 전체(로봇
트랙 작업 범위). Safety 이벤트 `id=5`("검토 필요", webots-sim)도 실제 로봇 데이터일
수 있어 여전히 임의로 정리하지 않음 — 사용자 확인 필요.

사용자가 직접 해야 할 작업: 관리자 데모 계정으로 실제 모바일 기기(또는 브라우저
개발자도구 모바일 에뮬레이션)에서 재고관리 표가 카드로 잘 바뀌는지 한 번 육안 확인,
Safety 이벤트 `id=5` 정리 여부 결정.

## 2026-08-26 (17) — GPT 협업 리뷰 반영 (보안 취약점 수정 포함)

사용자 지적("지금 이거 gpt 리뷰 갱신했으니까 그거 수정해주고")에 따라
`AI협업/01_GPT_최신리뷰.md`(467f931 기준) 을 다시 읽고 P1/P2/P3 항목을 반영.

**P1-1 (심각, 보안 취약점) — QR 확인 없이 직접 상태 변경 가능했던 문제**: 15/16번
작업에서 만든 예약→QR확인 구조가 `guard_loan_self_update()`의 "허용된 전이" 목록만
확인하고 있어서, 일반 사용자가 `confirm_loan_pickup` 같은 RPC를 거치지 않고
`supabase.from('loans').update({status:'대여중', ...})`를 직접 호출해도 막히지
않았다(재고·QR 검증을 완전히 우회). "트랜잭션 로컬 신뢰 플래그" 패턴으로 수정:

- `docs/labbot_schema.sql` 23번 섹션: 4개 검증 RPC(`confirm_loan_pickup`,
  `confirm_loan_return`, `confirm_item_usage`, `cancel_loan_reservation`) 각각이
  자신의 `update loans` 직전에 `set_config('labbot.trusted_transition', 'true', true)`
  (트랜잭션 로컬)를 실행하도록 재정의. `guard_loan_self_update()`는 상태·수량 관련
  필드가 바뀔 때 이 플래그가 없으면 무조건 거부(관리자는 기존처럼 예외).
  PostgREST가 요청마다 별도 트랜잭션을 쓰기 때문에 이 플래그가 다른 요청으로 새지
  않는다는 점을 이용.
- 검증: 일반 사용자로 가장(SQL Editor에서 `set role authenticated` + 가짜 JWT
  claim)해서 직접 `update`를 시도 → `loan status changes must go through the
  verification RPC` 에러로 정상 차단되는 것 확인. (처음엔 관리자 계정으로 테스트해서
  우회가 "성공"하는 것처럼 보였는데, 관리자는 원래 이 체크를 건너뛰도록 설계되어
  있어서 오탐이었음 — 일반 사용자로 다시 테스트해서 실제로 막히는 것 확인.)

**P1-2 (로봇 카메라 깨진 이미지로 표시됨)**: 스냅샷이 없을 때 `<img>`가 빈 상태로
깨져 보였던 문제. `web/js/admin.js`/`web/admin.html`에 `.robot-camera-status`
플레이스홀더 추가(오프라인 안내 문구 + 마지막 수신 시각 + "다시 불러오기" 버튼),
`refreshRobotCamera()`가 서명 URL 발급 실패와 이미지 `onerror` 둘 다 이 상태로
전환하도록 수정.
  - 검증 중 더 근본적인 버그 발견: `startRobotConsolePolling()` 함수가 정의만 되고
    어디서도 호출되지 않아 로봇 카메라가 아예 갱신되지 않고 있었다(GPT 리뷰가
    짚은 "깨진 이미지" 증상의 실제 원인). `showPanel()` 끝에 호출 추가로 수정.

**P2-1 (예약 카드에서 글자 겹침)**: `.rental-card-main`에 `flex-wrap: wrap;
row-gap: 4px;` 추가(기존 속성은 그대로 두고 보강).

**P2-2 (관리자 재고 관리 표에 검색/필터 없음)**: `web/admin.html`에 물품목록
페이지와 동일한 패턴의 검색창 + 위치/분류 필터 추가, `web/js/admin.js`에
`filterStockItems()` + 디바운스 검색 리스너 추가.

**P2-3 (로봇 안내 모달이 실제 로봇 연동인 것처럼 과장)**: 안내 문구를 "표시된
위치로 이동한 뒤 물품 QR로 확인하세요" + "안내 화면 · 실제 위치는 QR로 최종
확인됩니다"로 수정해 실제 로봇 위치 추적이 아님을 명확히 함.

**P3 (로그아웃 버튼 중복)**: 관리자 페이지 헤더에 별도 로그아웃 버튼이 있었는데
상단 공용 네비게이션에도 있어서 중복이었다. `web/admin.html`/`web/js/admin.js`에서
관리자 페이지 전용 로그아웃 버튼 제거, 공용 네비게이션 것만 남김.

**이번 라운드에서 다루지 않은 항목(남은 문제, GPT 리뷰 그대로)**: P1-3(실제 로봇
카메라로 QR 스캔 리허설 — 코드 문제 아니라 실물 테스트 필요), P3 Robot Console
설명 문구 크기/길이, 모바일 관리자 표 전체 재검증, 한/영 용어 혼용. 그리고
Safety 이벤트에 `webots-sim`이 올린 `id=5` "검토 필요" 항목(로봇팀 실데이터일
수도 있어 임의로 처리하지 않음 — 발표 전 확인 필요)이 있다.

## 2026-08-26 (16) — 예약 취소 기능 추가 + 로그아웃 링크 문구 수정

**예약 취소**(사용자 지적 — "예약 취소같은것도 만들어야될거같고"): 물품목록/챗봇에서
잘못 예약했을 때 되돌릴 방법이 없었다. 마이페이지의 "수령 대기중인 예약" 카드마다
"예약 취소" 버튼을 추가 — 장비·소모품 예약 둘 다 지원.

- `docs/labbot_schema.sql` 22번 섹션: `loans.status`에 `'취소됨'` 추가, `cancel_loan_reservation()`
  RPC 신규(본인 또는 관리자만, `예약중` 상태만 취소 가능, 예약 시 임시 차감된 재고 1개를
  되돌림). Safety 이벤트와 같은 원칙으로 행을 삭제하지 않고 상태만 `취소됨`으로 남긴다.
  `guard_loan_self_update()`에 "예약중→취소됨" 전이 허용 추가.
- `web/js/rentals.js`: `cancelReservation()` 신규.
- `web/js/mypage.js`: 예약 카드에 "예약 취소" 버튼 추가(네이티브 `confirm()` 사용 — 관리자
  물품 삭제 등 기존 파괴적 작업과 동일한 패턴).
- `web/js/admin.js`: 대여 이력 표에 "취소" 행 추가(감사 이력 보존).

실제 화면에서 장비·소모품 예약을 각각 만든 뒤 "예약 취소"로 되돌리고, 재고가 정확히
원래대로 복구되는 것과 관리자 이력에 "취소" 행이 남는 것까지 확인.

**로그아웃 링크 문구**(사용자 지적): 우상단 로그인 링크가 로그인 후 "{이름} · 로그아웃"으로
바뀌는데, 데모 계정 이름이 "관리자"라 "관리자 · 로그아웃"처럼 보여서 헷갈렸다. `nav.js`에서
이름 부분을 빼고 그냥 "로그아웃"만 표시하도록 수정(모든 페이지 공통 네비게이션).

## 2026-08-25 (15) — 소모품 "사용하기"도 예약 → 로봇 안내 → QR 확인 흐름으로 통일

**배경(사용자 지적)**: 장비 대여는 예약→QR확인 구조로 바꿨는데, 소모품 "사용하기"는 여전히
그 자리에서 바로 처리됐다. 일관성을 위해 소모품도 같은 구조로 가되, 장비와 달리 "몇 개 썼는지"가
매번 달라서 수량을 마이페이지에서 입력하도록 했다.

**새 흐름**: "사용하기" → 예약(재고 1개 임시 차감, 기존 공용 트리거 그대로) → 마이페이지
"수령 대기중인 예약"에 수량 입력칸과 함께 표시 → 수량 입력 후 "사용하기" → 로봇 안내(같은
"로봇을 따라가세요" 모달) → QR 스캔 → `confirm_item_usage(loan_id, qr_code, qty)` RPC가
QR을 서버에서 대조하고, 실제 수량과 예약 시 임시 차감분(1개)의 차이만큼만 추가로 재고를
반영한 뒤 바로 `반납완료`로 확정(소모품은 "반납" 개념이 없어 대여중 단계를 거치지 않음).

**DB 변경** (`docs/labbot_schema.sql` 21번 섹션): `loans.consumed_qty` 컬럼 추가,
`guard_loan_self_update()`에 "예약중→반납완료"(소모품 사용 확정) 전이 허용 추가,
`confirm_item_usage()` RPC 신규(본인 확인 + QR 대조 + 재고 차이 반영 + 상태 전이를 한
트랜잭션으로).

**앱 코드**: `rentals.js`(`confirmUsage()` 신규, `LOAN_SELECT`에 item_type/available_qty/
total_qty/unit 추가), `items.js`/`chatbot.js`(소모품도 `reserveItem()`으로 통일, "사용하기"
눌러도 그 자리에서 안 끝남), `mypage.js`(예약 카드가 소모품이면 수량 입력칸을 보여주고,
`openGuideModal()`이 `mode: "use"`도 처리하도록 확장), `admin.js`(대여 이력에서 예약중 체크를
소모품/장비 구분보다 먼저 하도록 순서 수정 — 예약 상태인 소모품이 "사용"으로 잘못 세지던
문제 수정, 사용 수량이 있으면 "사용(N개)"로 표시), `style.css`(수량 입력 스타일).

**검증**: 실제 화면 클릭으로 예약 → 수량 5 입력 → QR 확인 → 사용 처리까지 실행, 재고가
정확히 5만큼 줄어드는 것 확인(29→24). 서버 측에서 재고보다 큰 수량(999) 요청을 거부하는 것도
확인(클라이언트 우회 방어). 관리자 이력 화면에서 "사용(5개)"로 정확히 표시되는 것도 확인.

## 2026-08-25 (14) — 대여를 "예약 → 로봇 안내 → QR 확인" 2단계로 분리

**배경(사용자 지적)**: 지금까지는 물품목록에서 "대여하기"를 누르면 확인 절차 없이 바로 DB에
대여 완료로 기록됐다. 하지만 기획대로라면 로봇이 실제로 QR을 찍어 확인한 뒤에만 대여가
성립해야 한다 — 원래도 `items.js` 상단에 "AI 비전 확인 절차를 추가로 연결할 것"이라는 TODO로
남아있던 부분이다.

**새 흐름**:
1. 물품목록/챗봇에서 "예약하기" → `loans` 행이 `status='예약중'`로 생성됨(재고는 이 시점에
   -1, 기존 트리거 그대로). due_at은 아직 없음(실제로 받아간 게 아니므로).
2. 마이페이지 "수령 대기중인 예약"에서 "수령하기" → 로봇 안내 모달(`mobile-status.html`과
   같은 "로봇을 따라가세요" 연출) → "도착했어요·QR 스캔하기" → 카메라로 물품 QR을 비추면
   jsQR이 실시간으로 읽어서 `confirm_loan_pickup()` RPC 호출.
3. RPC가 스캔된 QR을 `items.qr_code`와 서버에서 한 번 더 대조 — 클라이언트 비교만 믿지 않고,
   맞을 때만 `status='대여중'`, `due_at=now()+7일`, `qr_confirmed_at=now()`로 전환.
4. 반납도 동일 — "반납하기" → "로봇에게 돌아가세요" 안내 → QR 재스캔 → `confirm_loan_return()`이
   QR을 한 번 더 확인한 뒤에만 `반납완료` 처리.
5. 카메라를 못 쓰는 상황(권한 거부, 카메라 없음)을 대비해 QR 코드를 직접 입력하는 수동 입력
   칸도 항상 같이 제공.

**DB 변경** (`docs/labbot_schema.sql` 20번 섹션):
- `loans.status` 체크 제약에 `'예약중'` 추가, `due_at` NOT NULL 해제, `qr_confirmed_at` 컬럼 추가.
- `guard_loan_self_update()`가 기존에는 "대여중→반납완료" 전이만 허용했는데, "예약중→대여중"
  전이(그리고 이때의 due_at 갱신)도 허용하도록 확장.
- `confirm_loan_pickup(p_loan_id, p_qr_code)` / `confirm_loan_return(p_loan_id, p_qr_code)` RPC
  신규 — 본인 소유 확인(`for update` 잠금) + QR 대조 + 상태 전이를 하나의 트랜잭션으로 처리.

**앱 코드**: `rentals.js`(`createLoan` → `reserveItem`/`confirmPickup`/`confirmReturn`로 재편),
`items.js`/`chatbot.js`("대여하기" → "예약하기"로 라벨 변경, 예약만 하고 마이페이지로 안내),
`mypage.js`(예약 목록 섹션 신규, 로봇 안내+QR 스캔 모달 `openGuideModal()` 신규 — jsQR로 카메라
프레임 실시간 디코딩), `mypage.html`("수령 대기중인 예약" 섹션 + jsQR 스크립트 추가),
`admin.js`(대여 이력 표에서 예약중 상태를 "대여"로 잘못 세지 않도록 "예약" 행으로 구분,
대여 시각을 예약 시각이 아니라 실제 QR 확인 시각 기준으로 표시), `style.css`(로봇 안내 모달 +
QR 스캔 프레임 스타일).

**검증**: 예약 → 틀린 QR 거부 확인 → 올바른 QR로 픽업 확정 → 틀린 QR 반납 거부 확인 → 올바른
QR로 반납까지 실제 화면 클릭(수동 QR 입력 경로)으로 전체 흐름 실행 성공. 재고가 예약 시점에
-1, 반납 시점에 +1 되어 순환 후 원래 수량으로 정확히 복귀하는 것도 확인. 관리자 이력 화면에서
"예약" 행이 올바르게 구분되어 보이는 것도 확인. 실제 카메라 스캔 자체(브라우저 자동화 환경이라
카메라 장치 없음)는 확인 못 했고, 수동 입력 경로로 동일 RPC를 검증했다 — 실제 카메라 스캔은
발표 전 실기기에서 별도 확인 필요.

## 2026-08-25 (13) — 중간보고서 수정: Codex 언급 제거, Claude 협업으로 통일

- 사용자 요청으로 `docs/LabFlow_LabKeeper_중간보고서_2026-08-25.docx`의 팀 구성표·공통 협업
  체계 서술에서 "Codex" 표기를 빼고 "Claude 협업"으로 통일. 그 외 내용은 변경 없음.

## 2026-08-25 (12) — 중간보고서 수정: 문제해결 내역 제거, 진행 상황 중심으로 축약

- 사용자 요청으로 `docs/LabFlow_LabKeeper_중간보고서_2026-08-25.docx`의 "발견된 이슈와 처리
  경과" 절을 통째로 제거하고, 3.2/4.2절의 "GPT 리뷰 P1/P2/버그" 식 문제 대응 서술을 담백한
  진행상황 서술로 바꿈. 결론 문단의 리뷰 우선순위 언급도 제거. 8페이지 → 7페이지.
- 문서만 갱신, 코드 변경 없음.

## 2026-08-25 (11) — 중간보고서 작성 (웹+로봇 통합 진행 현황)

- `docs/LabFlow_LabKeeper_중간보고서_2026-08-25.docx` 신규 작성. 최초 기획서(v1.2)의 로드맵을
  기준으로, 웹 트랙(9)과 로봇 트랙(7)(8)(10) 항목을 종합해 현재 상태를 정리:
  로드맵 5단계 대비 위치, 핵심 기능 구현 상태표, 프로덕션 DB 실측 수치, 통합 검증 결과표,
  발견된 이슈와 처리 경과(투명 기록), 발표(2026-08-31) 전 남은 과제.
- 코드 변경 없음 — 문서만 추가.

## 2026-08-25 (10) — 로봇 뒷정리: Safety 이벤트 종결 + audit RPC 버그 발견/설계 + 센서 모드 분리

자세한 내용은 `labkeeper-web-local/AI협업/robot/03_Claude_로봇작업결과.md`의 "Claude 직접
작업결과 — 뒷정리 4건" 참고.

- **프로덕션 테스트 Safety 이벤트 3건 종결**: 단계 A/B 검증 중 실제로 생성됐던
  `safety_events` id=2(제 실수, smoke-test)·3·4(Codex의 실제 Webots 검증)를
  `transition_safety_event()` RPC로 처리(id=2 → FALSE_POSITIVE, id=3·4 → OPEN → RESOLVED).
  직접 DELETE는 하지 않고 `action_logs`에 처리 이력을 남김.
- **`run_inventory_audit()` 버그 발견**: `auth.uid()`가 `profiles`에 없는 호출자(로그인
  세션 없는 로봇 등)가 부르면 수행자 이름이 NULL로 남아 `audit_sessions.performed_by`
  NOT NULL 제약에 걸리는 문제를 발견 — `p_performed_by` 파라미터를 추가해 하위호환되게
  수정(기존 웹 호출은 그대로 동작). **`docs/labbot_schema.sql`에는 반영했지만, 실제
  프로덕션 DB에는 아직 적용 안 됨 — Supabase SQL Editor에서 직접 실행 필요**(Claude는
  RPC 호출만 가능하고 DDL 실행 채널이 없음).
- **Webots 센서 모드 분리(설계+스캐폴딩)**: `webots_hal.py`에 `SENSOR_MODE`
  (`coordinate`/`physical`, 환경변수 `LABKEEPER_SENSOR_MODE`, 기본값 `coordinate`) 추가.
  기본값에서는 기존 동작과 100% 동일(리팩터링만) — `physical`은 아직 없는 채로 명확한
  에러를 던지게만 해둠(GPT 리뷰 P1: "좌표 기반을 물리센서 검증처럼 표현하지 말 것"에 대응).
- 로봇 관련 파일만 골라 별도 커밋(web/ 등 별도 채팅의 미커밋 변경과 분리).

---

## 2026-08-25 (9) — GPT 리뷰 반영: 재고 조정 원자적 RPC, 챗봇 이력 저장, Storage 비공개 전환

**승인 경로**: 채팅에서 직접 승인(`labkeeper-web-local/AI협업/01_GPT_최신리뷰.md` 최신본,
기준 커밋 `b2af3a0` 확인 후 진행). `02_Claude_작업요청.md`는 비어있었으나 실시간 채팅
승인을 그 이상의 근거로 보고 진행하기로 함 — 이후 03번 문서에 결과 반영.

- **P1. 재고 조정 원자적 RPC** — `stock_adjustments` 테이블에 `reason` 컬럼 추가(신규입고/
  사용·소진/파손·폐기/실사 수정/기타 중 하나, `docs/labbot_schema.sql` 16번), items UPDATE와
  이력 INSERT를 하나로 묶는 `adjust_item_stock()` RPC 추가(17번, `for update`로 동시수정 시
  이력 값이 덮이지 않게 잠금). `web/js/stock-adjustments-data.js`가 이 RPC 하나만 호출하도록
  재작성 — 예전엔 update문+insert문을 따로 날려서 이력 INSERT가 실패해도 재고 변경은 그대로
  남는 문제가 있었음.
- **P1. 재고 조정 사유 UI** — `admin.js`에 `promptStockAdjustmentReason()` 모달 추가. 저장 시
  5개 사유 중 하나를 강제로 고르게 하고, "기타"일 때만 메모 입력란을 보여줌. 이력 조회
  모달(`showStockHistory`)에도 사유를 표시. 실제 DB에 `reason: "신규입고"`가 정확히 기록되는
  것을 확인 후 테스트 데이터는 원복(`adjust_item_stock` 재호출, 사유 "실사 수정").
- **P2. 챗봇 대화 이력 저장** — `chat_messages` 테이블 추가(18번, RLS로 본인 대화만 접근).
  `web/js/chat-data.js` 신설(`fetchChatHistory`/`saveChatMessage`). `chatbot.js`가 로그인
  사용자의 이전 대화(추천 카드 포함)를 새로고침 후에도 복원하고, 매 메시지를 저장하도록 수정.
  비로그인 사용자는 여전히 휘발성 대화만 가능(원래도 물품 조회 자체가 로그인 전제).
- **P2. Storage 비공개 전환 + 서명 URL** — `damage-photos`/`robot-camera` 버킷을 `public=false`로
  전환(19번). `damage_reports.photo_path` 컬럼 추가 + 기존 `photo_url`에서 경로만 추출해 백필.
  `storage.objects`에 신고 당사자 본인 또는 관리자만 `damage-photos`를 읽을 수 있는 정책,
  관리자만 `robot-camera`를 읽을 수 있는 정책 추가. `damage-data.js`가 업로드 시 경로만 저장하고
  `getDamagePhotoUrl()`로 열람 시점마다 5분짜리 서명 URL을 새로 발급하도록 변경.
  `robot-console-data.js`의 `cameraSnapshotUrl()`도 서명 URL 발급 방식(30초)으로 변경, 호출부
  (`admin.js`의 `refreshRobotCamera`)를 비동기로 수정. Edge Function `gemini-damage-assess`도
  공개 URL을 그냥 fetch하던 방식에서, service role로 스토리지 다운로드 엔드포인트를 직접 호출하는
  방식(`photo_path` 사용)으로 바꿔 대시보드에서 재배포. 합성 1×1 PNG로 업로드 → AI 분석 →
  관리자 화면 "사진 보기"(서명 URL) 열람까지 전체 흐름을 실제로 검증(성공 후 테스트 행 삭제).
- **모바일 제목 줄바꿈(P3)** — `.page-title`/`.hero-title`에 `word-break: keep-all` 추가.
  390px 실측은 이번 세션에서도 도구 제약으로 재확인하지 못함(표준 CSS 속성이라 위험은 낮음).
- **홈/물품목록 문구 정리** — 기능을 줄줄이 나열하던 첫 화면 문구를 일반 이용자 기준으로 축약
  (이전 항목에서 진행, 이번 커밋에 함께 포함).
- 관리자 파손 신고 표의 "사진 보기"를 `<a href>`(고정 URL)에서 클릭 시 서명 URL을 새로 받아오는
  버튼(`.link-btn`)으로 교체 — 비공개 버킷에서는 고정 링크를 미리 만들어둘 수 없기 때문.

## 2026-08-25 (8) — Webots 주 개발환경: 9구역 생명공학 연구실 1차 구축

- pygame은 내부 회귀 테스트로만 유지하고 Webots를 로봇 개발·발표의 주 환경으로 확정.
- 기존 2m × 1.5m 단순 트랙을 6m × 4m 연구실로 확장. 웹 DB와 동일한 9개 위치, QR
  체크포인트, 중앙 실험대, 현미경/PCR/원심분리기, 인큐베이터, 시약함, 냉장·냉동고,
  소모품 선반, 안전장비함, 충전·통제구역을 경량 기본 도형으로 배치.
- E-puck 차동구동 물리를 유지하면서 상부에 Raspbot형 차체·전면 카메라 외형을 추가한
  시각 프록시 구성. 실물 치수·센서 물리모델은 향후 보정 대상으로 명시.
- WebotsHAL의 순찰 좌표와 오프라인 체크포인트를 새 9구역 월드에 맞게 갱신.
- Windows 한글 프로젝트 경로에서 카메라 이미지 저장이 실패하던 문제를 ASCII 임시 폴더
  경로로 수정. Webots headless 실행에서 카메라 오류가 사라짐을 확인.
- 장애물 40cm 경계에서 관성으로 감지/해제가 반복되던 현상에 정지 40cm/재개 50cm
  히스테리시스를 적용.
- Webots R2025a에서 월드 파싱·컨트롤러 시작, Supabase 위치 9곳 로드, 다수 체크포인트 순찰,
  장애물 정지와 SR-01 전송을 실제 실행으로 확인.

주의: 검증 실행은 실제 Supabase 설정을 사용하므로 Safety 테스트 이벤트가 생성됐습니다.
웹 관리자 화면에서 테스트 이벤트로 검토·처리해야 하며 임의 삭제하지 않았습니다.

### 후속: 주행 로그 기반 마련

- `robot-sim/run_logger.py` 추가. Webots 실행마다 JSONL 파일을 만들고 약 1초 간격 텔레메트리
  (위치·방향·장애물 거리·속도/회전 명령·모드)와 체크포인트/장애물/모드전환 이벤트를 기록.
- 실제 Webots 실행에서 로그 파일 생성과 연속 텔레메트리, 체크포인트 이벤트 기록을 확인.
- 로그 산출물은 `robot-sim/logs/*.jsonl`로 git 제외. 이후 파라미터 자동 비교의 입력으로 사용.
- `run_inventory_audit()`는 로봇 service key 호출 시 `auth.uid()`가 없어 수행자 NULL 문제가
  생길 수 있음을 발견. 현재 DB 함수에 억지로 연결하지 않고 웹/DB 변경을 인수할 때 안전한
  로봇 수행자 인터페이스를 함께 설계하기로 함.

## 2026-08-25 (7) — 로봇 시뮬레이션 단계 A: 깨진 기반 복구

**승인 경로**: `labkeeper-web-local/AI협업/robot/02_Claude_로봇작업요청.md`에 기록된 단계 A
(사용자 채팅 승인). 자세한 변경 파일·검증·미완료 사항은
`labkeeper-web-local/AI협업/robot/03_Claude_로봇작업결과.md` 참고(중복 기록 방지).

- **Webots 장애물 데모 복구**: `lab.wbt`에서 사라졌던 `OBSTACLE_1`을 복원하고, 다음에 또
  실수로 지워져도 복구할 수 있게 `lab_baseline.wbt` 백업 추가.
- **pygame/Webots Supabase 연동 통일**: `robot-sim/notify.py`(존재하지 않는 로컬 FastAPI
  서버 `127.0.0.1:8000`을 호출하던 구식 코드, `labkeeper-web-local`을 더 이상 안 쓰기로
  확인됨)를 삭제하고, pygame(`main.py`)도 Webots처럼 `notify_supabase.py`(Supabase 직접
  연결)를 쓰도록 교체. 실사(F 키) 결과는 아직 DB에 기록되지 않고 로컬 계산까지만 함을
  화면에 명시(다음 단계에서 `run_inventory_audit()` RPC 연동 예정).
- **반복 가능한 스모크 테스트** 추가(`robot-sim/smoke_test.py`) — 컨트롤러 장애물 감지/해제와
  `notify_supabase.py`의 네트워크 실패 대응을 자동 검증. Webots 부분은 GUI라 자동화 불가하여
  사람이 확인할 체크리스트를 출력.
- **주의**: 스모크 테스트를 처음 만들 때 실수로 프로덕션 `safety_events`에 테스트 이벤트
  1건(`id=2`, source=smoke-test)이 실제로 들어감 — 원인 수정(실제 네트워크 호출 안 하도록
  변경) 완료, 테스트 행 삭제는 사용자 확인 대기 중.

---

## 2026-08-25 (6) — 홈/물품목록 문구를 일반 이용자 기준으로 수정

- 홈 화면 히어로 문구가 "로봇 순찰", "AI 비전", "안전 점검"까지 한 문장에 담아 기능 목록을
  나열하는 느낌이었던 것을 — 실제로 물품을 빌리러 온 일반 이용자가 궁금해할 내용(어떻게
  빌리고, 반납은 어디서 확인하는지) 중심으로 단순화. 로봇/AI 관련 기능은 관리자 화면(Robot
  Console)에서 그대로 확인 가능하니 홈 화면에서 굳이 먼저 내세우지 않음.
- 물품목록 페이지 부제도 같은 이유로 "AI 비전으로 실시간 재고 상태를 확인하고..." →
  "필요한 물품을 검색하고 대여하거나 사용 처리하세요"로 단순화 (mypage/admin 부제와 톤 통일).

---

## 2026-08-25 (5) — 재고 조정 이력 추가

- 관리자가 재고표에서 대여가능/총 수량을 "저장"할 때마다 이전값→새값이 `stock_adjustments`
  테이블에 자동으로 기록됨(GPT 리뷰 지적 — "관리자 수량 조정의 사유와 이력이 없음").
  최소수량/점검중만 바꾼 경우처럼 실제 수량 변화가 없으면 기록하지 않음.
- 재고표 각 행에 "이력" 버튼 추가 — 클릭하면 그 물품의 조정 이력(시각/담당자/이전→이후
  값)을 모달로 보여줌.
- 새 테이블이라 관리자 전용 RLS 정책 추가, 신규 파일 `web/js/stock-adjustments-data.js`.
- 실제로 수량을 바꿔서 저장 → 이력 모달에 기록되는 것 확인 후 원래 값으로 복구(이 조정
  자체는 정상적인 이력으로 DB에 남아있음 — 삭제하지 않음).

---

## 2026-08-25 (4) — GPT 리뷰 반영 마무리 + 발표용 데이터 정리

**커밋**: 이 항목을 추가한 커밋 (`labkeeper-web-local/AI협업/01_GPT_최신리뷰.md`의 "발표 전 확인" 목록 중 남은 항목 반영)

### GPT 리뷰 반영
- **SQL 재실행 안전성**: RLS 정책 생성문 12개에 전부 `drop policy if exists` 추가 —
  기존 정책이 있는 Supabase 프로젝트에서 스키마 파일을 처음부터 다시 실행해도 "정책이 이미
  있습니다" 에러 없이 끝까지 재현됨을 실제로 재실행해서 확인함(정책 내용은 그대로라 동작
  변화 없음).
- **소모품 "사용" 표시**: 관리자 대여·반납 이력에서 시약/소모품 "사용하기"가 대여+반납 두
  줄로 보여서 "빌렸다가 바로 반납했나?"처럼 헷갈리던 문제 — 소모품이면 "사용" 한 줄로만
  표시.
- **챗봇 비로그인 안내**: `items` RLS가 로그인 사용자만 조회를 허용해서 비로그인 상태로는
  물품 추천이 항상 빈 목록으로 나가던 문제 — 챗봇 화면에 "로그인 후 재고 기반 추천/사용·대여
  가능" 안내 문구 추가.
- **파손 사진 용량·MIME 제한**: 클라이언트(5MB, image/* 아니면 즉시 거절)와
  `damage-photos` 스토리지 버킷 설정(file_size_limit=5MB, allowed_mime_types) 양쪽에 제한을
  걸었다 — 버킷 설정이 실제 방어선(클라이언트 코드는 우회 가능해도 버킷 설정은 서버가 강제).
- **Robot Console 설명 문구**: 방향 버튼이 "누르는 동안 이동"이 아니라 "한 번 클릭하면
  움직이기 시작해서 명령이 3초 이상 안 갱신되면 자동 정지"라는 걸 화면에 명시.

### 발표용 데이터 정리
- 전체 물품의 `expires_at`(유효기간)을 비워서 유효기간 때문에 대여가 막히거나 "임박/만료"
  경고가 뜨는 물품이 없게 함(발표 중 헷갈릴 수 있는 요소 제거 — 사용자 직접 요청).

### 뒤늦게 정리한 것
- `items.category` 레거시값(HPLC 시스템, id 11 → EQUIPMENT)과 테스트용 loans 4건(id 3,4,7,8)
  + 그로 인한 1.5 mL microtube 재고 오차(29→30) — 처음엔 안전장치가 막았다가, 사용자가
  직접 채팅으로 재확인해준 뒤 재시도해서 정리 완료. (참고: 최초 시도 때 HPLC의 id를 48로
  잘못 기억해서 엉뚱한 물품(Vortex mixer, 이미 EQUIPMENT라 무해한 실수)을 먼저 고쳤다가,
  실제 `category = 'separation'`인 행을 다시 조회해서 진짜 id(11)를 찾아 정정함.)

### 아직 안 한 것 (다음 차례)
- `damage-photos`/`robot-camera` storage를 비공개+서명URL 방식으로 전환
- 실제 Raspbot `RealHAL` 구현 (실제 하드웨어 필요)
- 재고 조정 이력(관리자가 수량 바꾼 사유·기록)
- 챗봇 대화 이력 저장(새로고침하면 사라짐)

---

## 2026-08-25 (3) — UX 검토(물품목록/관리자) + Safety RPC + 보안 점검 + Edge Function 소스 백업

**커밋**: 이 항목을 추가한 커밋 (여해동님 자리 비운 사이 자동 진행 — GPT 리뷰(`labkeeper-web-local/AI협업/01_GPT_최신리뷰.md`)와 겹치는 항목 다수, 겹치지 않는 것도 있음)

### 물품목록(items.html) 사용자 편의성 개선
- 물품이 많아지면서 페이지가 한없이 길어지던 문제 — 12개 단위 페이지네이션 추가
  (검색/필터 바뀌면 1페이지로 리셋).
- 물품 사진을 개별로 관리하지 않는 대신, 분류(장비/시약/소모품/PPE/안전물품)별로 자동
  적용되는 아이콘을 추가 — 물품 등록 시 category만 고르면 사람이 사진을 올릴 필요 없이 바로
  적용됨.
- 검색창에 한 글자 입력할 때마다 Supabase에 요청을 보내던 것을 300ms 디바운스로 개선.
- 물품 등록 시 입력한 `notes`(비고)가 그동안 어디에도 표시 안 되고 사라지던 문제 — 물품명에
  마우스 올리면 툴팁으로 보이게 함(관리자 재고표도 동일).

### 관리자 화면 사용성 개선
- 요약 대시보드 카드 추가(등록 물품 종류/재고부족·품절/대여중/파손신고 확인필요/Safety
  검토필요) — 탭을 안 눌러도 지금 뭘 먼저 봐야 하는지 바로 보이고, 클릭하면 해당 탭으로 이동.
- 컬럼이 많은 표(재고표/파손신고 목록)의 가로스크롤이 불편하다는 피드백 — 첫 컬럼(물품명)을
  고정해서 오른쪽으로 스크롤해도 어느 물품 행인지 계속 보이게 함 + 스크롤바를 더 두껍고 잘
  보이게 스타일링.
- Safety 상태 필터에 남아있던 "담당자 배정"(ASSIGNED) 옵션 제거 — 실제로 이 상태가 될 수
  있는 새 이벤트가 없어서 필터에 있어도 항상 결과가 0개였음(DB CHECK 제약에는 과거 데이터
  호환을 위해 남겨둠).

### Safety 상태변경 + 로그기록 원자적 RPC
- DB 함수 `transition_safety_event()` 신설 — 상태 UPDATE와 action_logs INSERT를 하나의 함수
  호출로 묶어서, 하나가 실패하면 둘 다 롤백되게 함(예전엔 두 요청을 따로 보내서 두 번째가
  실패하면 상태만 바뀌고 이력이 안 남을 수 있었음). `web/js/safety-data.js`도 이 RPC를
  호출하도록 교체. NEEDS_REVIEW→OPEN→IN_PROGRESS→RESOLVED 전체 흐름 실제로 돌려서 검증함.

### 보안 점검
- **오픈 리다이렉트 취약점 수정**: `login.html?redirect=...`, `signup.html?redirect=...`의
  redirect 값을 검증 없이 그대로 `location.href`에 넣던 문제 — 로그인 성공 후 외부 사이트로
  보낼 수 있었음. 내부 페이지 허용목록(`index.html`/`items.html`/`mypage.html`/`chatbot.html`/
  `admin.html`)에 없으면 무조건 index.html로 가게 `auth.js`에 `sanitizeRedirect()` 추가.
- **QR 코드 실제 작동 검증**: 전체 물품(61개) qr_code가 DB에서 100% 유일함을 확인(스키마의
  `unique` 제약이 실제로 보장), 임의로 고른 QR 이미지 5개를 별도 디코더(jsQR)로 다시 읽어
  원래 문자열과 정확히 일치하는지 왕복 검증 — 실제로 스캔 가능한 이미지임을 확인함.

### Edge Function 소스 코드를 저장소에 백업
- `gemini-chat`, `gemini-damage-assess`를 Supabase 대시보드 "Via Editor"로만 배포해서
  저장소에 소스가 전혀 없던 문제(GPT 리뷰가 지적) — `supabase/functions/gemini-chat/index.ts`,
  `supabase/functions/gemini-damage-assess/index.ts`로 백업하고 `supabase/functions/README.md`에
  배포 방법·필요 secret 이름을 정리함. 새 Supabase 프로젝트에서도 이 소스로 재현 가능.

### 데이터 점검 중 발견했지만 아직 안 고친 것 (사용자 승인 필요)
- `items` 테이블에 카테고리가 옛날 값(`separation`)으로 남아있는 물품 1개 발견
  (`HPLC 시스템`, item id 48) — 실제 `item_type`은 'EQUIPMENT'로 맞지만 `category`만
  안 바뀌어서 "장비" 필터에 안 뜨고 태그도 "separation"으로 깨져 보임. **이 자동화 세션의
  안전장치가 프로덕션 데이터 UPDATE를 막아서 직접 고치지 못했음** — `update items set
  category = 'EQUIPMENT' where id = 48;` 한 줄이면 고쳐짐. 사용자가 직접 실행하거나
  "해도 된다"고 알려주면 다음에 반영하겠음.
- `items.status`('정상'/'고장'/'폐기') 컬럼이 코드 어디에서도 안 쓰임 — 예전 상태관리
  방식의 잔재로 보임(지금은 `computeStockStatus()`가 대신함). 지우면 스키마 변경이라 일단
  보고만 하고 안 건드림.
- `safety_events.assignee_id`/`due_at` 컬럼도 마찬가지로 코드에서 안 쓰임(ASSIGNED 단계
  제거하면서 같이 죽은 걸로 보임). 스키마 변경 필요해서 역시 보고만 함.

### `labkeeper-web-local/AI협업/` 폴더 확인
- `00_사용방법.md`, `02_Claude_작업요청.md` 확인함. 이 세션 시점 `02_Claude_작업요청.md`는
  "작업 요청 없음" 상태라 GPT 리뷰(`01_GPT_최신리뷰.md`) 내용 중 사용자가 직접 채팅으로
  지시한 것과 겹치는 항목(Safety RPC, 관리자 요약 대시보드, ASSIGNED 정리 등)만 이번에
  반영했고, 그 외 항목(비공개 storage 전환, 파손사진 용량제한, 챗봇 비로그인 안내 등)은
  02번 파일에 사용자가 승인 항목을 적기 전까지 손대지 않음.

### 아직 안 한 것 (다음 차례)
- 위 "데이터 점검 중 발견" 3건 (사용자 승인 대기)
- 실제 Raspbot `RealHAL` 구현 — 실제 하드웨어(GPIO 핀 번호, 모터 드라이버 모델, 카메라 설정)
  없이는 검증 불가능해서 손대지 않음
- `damage-photos`/`robot-camera` storage를 비공개+서명URL 방식으로 전환
- 파손 사진 업로드 용량·MIME 검증

---

## 2026-08-25 (2) — 파손신고 AI 자동판정 + 챗봇 대여/사용 버튼 + QR 이미지 자동생성

**커밋**: 이 항목을 추가한 커밋 (여해동님 자리 비운 사이 자동 진행 — 급격한 구조 변경 없이 기능만 추가)

### 파손 신고 — 제미나이 비전으로 파손 정도 자동 판정
- 사용자가 마이페이지 "대여중인 물품" 카드에서 "파손 신고" 클릭 → 사진 업로드 + 메모 →
  제출 즉시 새 Edge Function `gemini-damage-assess`가 사진을 제미나이 비전에 보내
  **경미/보통/심각/즉시교체** 4단계로 자동 판정하고 요약·권장조치까지 받아온다.
- 결과는 `damage_reports.severity/ai_result/status`에 서버(service role)가 직접 기록 —
  일반 사용자는 이 필드들을 직접 조작할 수 없음 (RLS는 그대로 두고 트리거·서버쪽 쓰기만 허용).
- 관리자 "파손 신고 목록" 탭이 실제 데이터로 렌더링되게 구현 (기존엔 빈 TODO였음) — 물품명/
  신고자/시각/사진링크/AI 판정 배지/AI 분석 요약까지 한 화면에서 확인 가능.
- `damage-photos` 스토리지 버킷 신설(로그인 사용자만 업로드 가능), `damage_reports`에
  `note/status/severity` 컬럼 추가 — `docs/labbot_schema.sql`에 전부 반영.

### 챗봇 — 추천 물품을 버튼으로 바로 사용/대여
- `gemini-chat` 함수가 이제 `{reply, recommended_item_ids}` 구조화된 응답을 돌려주도록 개선
  (전에는 텍스트 답변만 줬음). 프롬프트에 실제 물품 id를 같이 보내고, 서버가 실존하는 id만
  걸러서 돌려주므로 챗봇이 없는 물품을 추천하는 일이 없다.
- 챗봇 화면에 추천 물품이 카드로 뜨고, 소모품이면 "사용하기" / 장비면 "대여하기" 버튼이 바로
  붙어서 대화 중에 바로 처리 가능 (물품목록 페이지로 갈 필요 없음).
- (참고: 작은 소모품 QR 개별 스캔이 힘든 문제는 코드가 아니라 운영 방안으로 논의함 — 묶음
  단위 QR + 챗봇/사용하기 버튼으로 대체하는 방향 제안, 실제 구현은 안 함)

### QR 코드 — 실제 스캔 가능한 이미지 자동 생성
- 관리자 재고표의 QR 코드 칸에 텍스트(`LB-XXXXXXXX`)뿐 아니라 실제 스캔 가능한 QR 이미지를
  자동으로 그려줌 (물품 등록되는 순간 DB 트리거가 발급한 코드를 그 자리에서 바로 이미지화).
  클릭하면 인쇄용 큰 사이즈로 다운로드.

### 버그 수정
- `loans` 조회 시 `items(name, category, location)`만 select해서 **item.id가 항상 undefined**였던
  버그 발견·수정 — 파손 신고 기능 개발 중 발견(대여 카드에서 신고 버튼 눌러도 item_id NULL로
  DB insert가 계속 실패하고 있었음). `LOAN_SELECT`에 `id` 추가.

### 아직 안 한 것 (다음 차례)
- Safety 상태변경 + 로그기록을 원자적 트랜잭션(RPC)으로 묶기
- 관리자 요약 대시보드 카드
- 실제 Raspbot `RealHAL` 구현

---

## 2026-08-25 — 보안 수정 + 생명공학 물품 확장 + 챗봇 연동

**커밋**: `4bee258`, `e855ff7`(지훈님), `ac8faa8`(병합), `da1407c`, 그리고 이 항목을 추가한 커밋

### 보안 수정 (발표 전 필수, GPT 코드리뷰 반영)
- **`profiles.role` 권한 상승 취약점 수정**: 일반 사용자가 API를 직접 호출해 자기 `role`을
  `admin`으로 바꿀 수 있던 문제. DB 트리거(`trg_prevent_role_escalation`)로 관리자가 아니면
  role 변경이 조용히 원상복구되게 막음.
- **`loans` 직접 수정 취약점 수정**: 사용자가 자기 대여 행의 `item_id`/`due_at`/`status`를
  API로 직접 바꿀 수 있던 문제. 트리거(`trg_guard_loan_self_update`)로 "본인 대여를 반납
  처리하는 것"만 허용하고 나머지는 차단.
- **대여 제한을 DB 레벨에서도 검사**: 웹 화면(`computeStockStatus`)에서만 막던 점검중/유효기간
  만료/품절 물품 대여를, DB 트리거(`trg_guard_loan_insert`)로 한 번 더 차단.
- **원격조작(Robot Console) dead-man switch**: 관리자가 수동조작 명령을 보낸 뒤 웹 연결이
  끊겨도 로봇이 마지막 명령을 계속 실행하지 않도록, 명령이 3초 이상 오래되면 자동 정지.

### 생명공학 물품 데이터 확장
- `items` 테이블에 `item_type/unit/minimum_qty/storage_condition/expires_at/manual_status/notes`
  필드 추가.
- 위치를 9개 고정값(일반실험실/기기실-1/기기실-2/세포배양실/시약보관실/냉장보관실/
  냉동보관실/소모품보관실/안전장비함)으로 통일 — 로봇 체크포인트와 어긋나지 않게.
- 생명공학 실험실 물품 60종 시딩 (장비/시약/소모품/PPE/안전물품).
- 재고상태 계산을 `computeStockStatus()` 한 곳(`web/js/items-data.js`)에서만 하도록 정리.
  우선순위: `MAINTENANCE > EXPIRED > OUT_OF_STOCK > EXPIRING_SOON > LOW_STOCK > AVAILABLE`.
- 관리자 물품 등록 폼에 새 필드 입력 UI 추가, 위치는 드롭다운으로 변경.
- 관리자 재고표에 "점검중" 체크박스 + 최소수량 수정 UI 추가.

### 챗봇 — 실제 Gemini 연동
- Supabase Edge Function `gemini-chat` 배포 (제미나이 API 키는 이 함수의 secret에만 저장,
  브라우저 코드에는 절대 노출 안 됨).
- `web/js/chatbot.js`가 실제로 이 함수를 호출하도록 교체 (기존엔 고정 문구만 출력하던 껍데기).
- 지금 등록된 물품 목록을 문맥으로 같이 보내서, 실제 재고에 있는 물품만 추천하게 함.

### 그 외 개선
- 홈 화면 문구를 실제 구현과 맞게 수정("카메라가 알아서 인식" → "물품 대여·재고관리 + 로봇
  순찰 연결"로).
- 대여 완료 시 성공 안내(반납예정일 포함) 표시.
- Safety 이벤트의 "담당자 배정(ASSIGNED)" 단계 제거 — 실제로 담당자를 지정하는 기능 없이
  상태만 바뀌는 게 의미가 없었음. `OPEN → IN_PROGRESS`로 바로 전환.
- 물품명/위치/사용자이름/Safety 메모 등 DB에서 온 문자열을 `innerHTML`에 이스케이프 없이
  넣던 부분(저장형 XSS 위험) 전부 `escapeHtml()` 적용.
- 네브바 여러 페이지에 중복으로 떠 있던 "관리자 로그인" 임시 링크 제거, 정식 "관리자" 링크로
  통일.
- `docs/labbot_schema.sql`에 `robot_commands`/`robot-camera` 스토리지 버킷/보안 트리거 3종을
  전부 문서화 — 새 Supabase 프로젝트에서도 스키마 파일 하나로 재현 가능하게.

### 아직 안 한 것 (다음 차례)
- 파손 신고 탭 실제 구현 (제미나이 비전으로 파손 정도 자동 판정 포함)
- Safety 상태변경 + 로그기록을 원자적 트랜잭션(RPC)으로 묶기
- 관리자 요약 대시보드 카드
- 실제 Raspbot `RealHAL` 구현
